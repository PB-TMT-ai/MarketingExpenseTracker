---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tsconfig.json
  - drizzle.config.ts
  - .env.local
  - .env.example
  - .gitignore
  - app/layout.tsx
  - app/login/page.tsx
  - app/(app)/layout.tsx
  - app/(app)/page.tsx
  - lib/db/index.ts
  - lib/auth/session.ts
  - lib/auth/password.ts
  - lib/actions/auth.ts
  - lib/actions/spike.ts
  - proxy.ts
autonomous: false
requirements: [ACCESS-01, ACCESS-02]
user_setup:
  - service: local-env
    why: "Shared-password gate + cookie signing + local DB path are read from server-only env vars"
    env_vars:
      - name: APP_PASSWORD
        source: "Choose any value; set in .env.local (gitignored). NEVER prefix NEXT_PUBLIC_."
      - name: SESSION_SECRET
        source: "Generate >=32 random bytes (e.g. `openssl rand -base64 32`); set in .env.local. NEVER prefix NEXT_PUBLIC_."
      - name: DATABASE_URL
        source: "Local default `./.pglite` (no value needed to run locally). Supabase pooled URL only at deploy time."

must_haves:
  truths:
    - "Running `npm run dev` starts the app with no cloud dependency."
    - "Visiting any protected route without a valid session cookie redirects to /login."
    - "Entering the correct shared password sets a signed httpOnly cookie and admits the user to the app shell."
    - "Entering a wrong password shows an inline error and sets no cookie."
    - "A Server Action proves a real round-trip to the embedded PGlite database (SELECT 1 returns 1)."
    - "A visible Logout clears the session cookie and returns the user to /login."
  artifacts:
    - path: "lib/db/index.ts"
      provides: "Dual-driver Drizzle instance branching on DATABASE_URL (PGlite local / postgres-js Supabase), cached on globalThis"
      contains: "globalThis"
    - path: "lib/auth/session.ts"
      provides: "jose HS256 mintSession/verifySession over SESSION_SECRET"
      exports: ["mintSession", "verifySession"]
    - path: "lib/actions/auth.ts"
      provides: "login + logout Server Actions (constant-time compare, cookie set/clear)"
      exports: ["login", "logout"]
    - path: "proxy.ts"
      provides: "Node-runtime route gate verifying the jose cookie, excluding /login + static"
      exports: ["default", "config"]
    - path: "lib/actions/spike.ts"
      provides: "Server Action that runs SELECT 1 against PGlite to prove the stack end-to-end"
      exports: ["dbSpike"]
  key_links:
    - from: "proxy.ts"
      to: "lib/auth/session.ts"
      via: "verifySession(cookie)"
      pattern: "verifySession"
    - from: "lib/actions/auth.ts"
      to: "lib/auth/session.ts"
      via: "mintSession() on correct password"
      pattern: "mintSession"
    - from: "lib/actions/spike.ts"
      to: "lib/db/index.ts"
      via: "db.execute(sql`select 1`)"
      pattern: "select 1"
---

<objective>
Stand up the Walking Skeleton: the thinnest complete vertical slice that proves the whole stack runs locally. Scaffold Next.js 16, wire the dual-driver Drizzle DB seam (PGlite local / Supabase-postgres.js prod) behind a single `DATABASE_URL`, lock the PGlite-under-Turbopack run incantation with a `SELECT 1` spike, and gate the app behind the shared-password `jose` cookie (`proxy.ts` Node runtime + login/logout Server Actions + a per-action re-check).

Purpose: every later phase writes into this skeleton; nothing reads or runs without the DB seam and the auth gate. Locking the local-run incantation first (RESEARCH Open Question 1) de-risks the schema work in Plan 02.
Output: A locally runnable Next 16 app with a working password gate, a proven PGlite round-trip, and the directory backbone in SKELETON.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/SKELETON.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Contracts this plan CREATES for downstream plans. Executors implement these signatures directly. -->

lib/db/index.ts — exports `db` (Drizzle instance). Branch logic:
  const url = process.env.DATABASE_URL ?? "./.pglite";
  isPg = /^postgres(ql)?:\/\//.test(url)
  isPg → drizzle-orm/postgres-js over postgres(url, { prepare: false })
  else  → drizzle-orm/pglite over new PGlite(url)
  cached on (globalThis as any).__db so Next dev hot-reload reuses PGlite's ONE connection.
  Schema import is `import * as schema from "./schema"` — schema.ts is created in Plan 02; for THIS plan
  the import may point at an empty `export {}` placeholder schema file is NOT created here — instead import
  schema lazily/optionally so db/index.ts compiles before Plan 02 (use `{}` as schema if schema.ts absent).
  NOTE for executor: create lib/db/index.ts so it does not hard-fail if schema has no tables yet.

lib/auth/session.ts:
  export async function mintSession(): Promise<string>   // jose SignJWT HS256, 30-day exp, secret = SESSION_SECRET
  export async function verifySession(token: string): Promise<boolean>  // jwtVerify; false on any throw

lib/actions/auth.ts ("use server"):
  export async function login(prevState: unknown, formData: FormData): Promise<{ ok?: true; error?: string }>
  export async function logout(): Promise<void>   // clears cookie, redirect("/login")

lib/actions/spike.ts ("use server"):
  export async function dbSpike(): Promise<{ ok: boolean; value: number | null; error?: string }>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next 16 + dual-driver DB seam + lock the PGlite run incantation (spike)</name>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (§Standard Stack install delta; §Pattern 1 dual-driver db/index.ts; Pitfall 2 Turbopack/PGlite; Pitfall 3 globalThis singleton; §Next 16 scaffold; Environment Availability)
    - .planning/PROJECT.md (locked stack + versions)
  </read_first>
  <action>
    Scaffold with `npx create-next-app@latest . --typescript --tailwind --eslint --app` (scaffold INTO the existing repo root — keep CLAUDE.md and .planning/; Turbopack, Tailwind v4, ESLint flat config are the Next 16 defaults). Pin `next@16.2.7`, `react`/`react-dom@19.2.7`. Install runtime deps `drizzle-orm@0.45.2 @electric-sql/pglite@0.5.1 postgres@3.4.9 jose@6.2.3 zod@4.4.3` and dev deps `drizzle-kit@0.31.10 dotenv-cli`. Do NOT install SheetJS / AG Grid / shadcn (those are Phases 2-3).
    Add `serverExternalPackages: ["@electric-sql/pglite"]` to `next.config.ts` (Pitfall 2). Create `lib/db/index.ts` exactly per the `<interfaces>` block: branch on `DATABASE_URL` (regex `/^postgres(ql)?:\/\//`), PGlite path → `new PGlite(url)` + `drizzle-orm/pglite`, postgres:// → `postgres(url,{prepare:false})` + `drizzle-orm/postgres-js`, cache on `globalThis.__db`. Make it compile WITHOUT a populated schema (Plan 02 adds tables) — import schema defensively.
    Create `lib/actions/spike.ts` exporting `dbSpike()` ("use server") that runs `db.execute(sql\`select 1 as v\`)` and returns `{ ok, value }`. Add `.env.local` (gitignored) with `DATABASE_URL=./.pglite`, `APP_PASSWORD=<dev value>`, `SESSION_SECRET=<32+ bytes>`; add `.env.example` documenting all three as server-only (warn: never `NEXT_PUBLIC_`). Add `.pglite`, `.env.local`, `.next`, `node_modules` to `.gitignore`. Add npm scripts `dev`, `db:generate` (`drizzle-kit generate`), `db:migrate:local` (placeholder calling the Plan 02 migrate entry), `db:studio`.
    LOCK THE INCANTATION: run `npm run dev`, invoke `dbSpike` (temporary button on a throwaway route or a Node script that imports the action), confirm it returns value 1. If Turbopack throws WASM/"Unknown module type" errors, confirm `serverExternalPackages` resolves it; if still failing, switch the `dev` script to `next dev --webpack` and record which lever was needed in the SUMMARY.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -qiE "compiled|success|✓" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` lists next@16.2.7, react@19.2.7, drizzle-orm@0.45.2, @electric-sql/pglite@0.5.1, postgres@3.4.9, jose@6.2.3, zod@4.4.3; SheetJS/AG Grid are absent.
    - `next.config.ts` contains `serverExternalPackages: ["@electric-sql/pglite"]`.
    - `lib/db/index.ts` exports `db`, contains the `DATABASE_URL` regex branch and `globalThis` cache, and type-checks with no schema tables yet.
    - Invoking `dbSpike()` under the running dev server returns `{ ok: true, value: 1 }` (the local-run incantation is proven; the chosen Turbopack-vs-webpack lever is recorded in the SUMMARY).
    - `.env.local` is gitignored; `.env.example` documents APP_PASSWORD / SESSION_SECRET / DATABASE_URL as server-only with a "never NEXT_PUBLIC_" warning.
  </acceptance_criteria>
  <done>App scaffolds, builds, and a Server Action proves a live PGlite `SELECT 1` round-trip locally; the working dev incantation is recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared-password gate — jose session, login/logout Server Actions, login page</name>
  <read_first>
    - lib/db/index.ts (created Task 1 — for the eventual data layer; not edited here)
    - .planning/phases/01-foundation/01-RESEARCH.md (§Pattern 4 proxy.ts + jose + login action; Pitfall 4 async cookies(); §Security Domain V2/V3/V6)
    - .planning/research/PITFALLS.md (Pitfall 10 shared-password misconfig; security mistakes table)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-12, D-13)
  </read_first>
  <behavior>
    - verifySession(mintSession()) resolves true; verifySession("garbage") resolves false; verifySession of a token signed with a different secret resolves false.
    - login with formData password === process.env.APP_PASSWORD returns { ok: true } and sets a "session" cookie (httpOnly, secure, sameSite=lax, path=/, maxAge 30d).
    - login with a wrong password returns { error: "Incorrect password" } and sets NO cookie.
    - Password comparison uses node:crypto timingSafeEqual with a length guard (constant-time), not ===.
  </behavior>
  <action>
    Create `lib/auth/session.ts` per `<interfaces>`: `jose` `SignJWT`/`jwtVerify` HS256 over `new TextEncoder().encode(process.env.SESSION_SECRET)`; `MAX_AGE = 60*60*24*30` (30-day sliding, D-13). Create `lib/auth/password.ts` exporting a constant-time `verifyPassword(input: string): boolean` using `node:crypto` `timingSafeEqual` with a length check against `process.env.APP_PASSWORD`.
    Create `lib/actions/auth.ts` ("use server"): `login(prevState, formData)` reads `password`, calls `verifyPassword`; on fail returns `{ error: "Incorrect password" }` (inline, no lockout, D-13); on success `const jar = await cookies()` (Next 16 async cookies, Pitfall 4) and `jar.set("session", await mintSession(), { httpOnly:true, secure:true, sameSite:"lax", path:"/", maxAge: 60*60*24*30 })`, returns `{ ok: true }`. `logout()` awaits `cookies()`, deletes "session", `redirect("/login")`.
    Create `app/login/page.tsx`: a client form using `useActionState(login, {})` posting `password`; renders the inline error; on `{ ok }` navigates to `/`. Validate the `password` field presence with a Zod schema before compare.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create lib/auth/session.test.ts first; then: npx vitest run lib/auth/session.test.ts lib/actions/auth.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/auth/session.ts` exports `mintSession`/`verifySession`; a round-trip test passes and a wrong-secret token fails verification.
    - `lib/auth/password.ts` uses `timingSafeEqual` with a length guard (no `===` on the secret).
    - `lib/actions/auth.ts` `login` sets the cookie only on the correct password and returns `{ error }` otherwise; `cookies()` is awaited.
    - `app/login/page.tsx` renders the form, shows the inline error on wrong password, and routes to `/` on success.
    - No `NEXT_PUBLIC_` prefix appears on `APP_PASSWORD` or `SESSION_SECRET` anywhere (grep the repo).
  </acceptance_criteria>
  <done>A correct password mints a signed cookie and a wrong one shows an inline error; session sign/verify is unit-tested.</done>
</task>

<task type="auto">
  <name>Task 3: proxy.ts route gate + protected app shell with Logout</name>
  <read_first>
    - lib/auth/session.ts (created Task 2 — proxy verifies via verifySession)
    - lib/actions/auth.ts (created Task 2 — shell wires logout)
    - .planning/phases/01-foundation/01-RESEARCH.md (§Pattern 4 proxy.ts matcher; §Anti-Patterns "treating proxy as the security boundary"; State of the Art proxy rename)
    - .planning/research/ARCHITECTURE.md (Recommended Project Structure — (app) protected group)
  </read_first>
  <action>
    Create `proxy.ts` at the repo root (Next 16 Node-runtime gate; replaces middleware.ts): exported `default async function proxy(req)` returns `NextResponse.next()` for paths starting `/login` or `/_next`; otherwise reads `req.cookies.get("session")?.value` and `await verifySession(token)` → `next()` if valid, else `NextResponse.redirect(url with pathname "/login")`. Export `config = { matcher: ["/((?!_next/static|_next/image|favicon.ico).*)"] }`. Do NOT import PGlite or any DB code into proxy.ts (it is pure cookie crypto — Anti-Patterns).
    Create the protected group: `app/(app)/layout.tsx` is the app shell — a top bar with the app name, a placeholder slot where the period switcher will mount (added in Plan 04), and a Logout control wired to the `logout` Server Action (a form whose action is `logout`). `app/(app)/page.tsx` is a placeholder landing ("Foundation ready — periods & items below"; the real dashboard is Phase 4). Add a defense-in-depth note: the shell is a Server Component that ALSO calls `verifySession` on its own cookie and redirects to `/login` if absent (per-render re-check; do not trust proxy alone — CVE-2025-29927 lesson). Update `app/layout.tsx` only for the root html/body + global metadata if the scaffold did not already.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -qiE "compiled|success|✓" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `proxy.ts` exists at repo root, exports `default` + `config`, calls `verifySession`, excludes `/login` and `/_next`, and imports NO database code.
    - `app/(app)/layout.tsx` renders the shell with a Logout form bound to the `logout` action and a placeholder for the period switcher.
    - The `(app)` shell re-verifies the session server-side (defense-in-depth) independent of proxy.ts.
    - Manual: with no cookie, GET `/` 302-redirects to `/login`; after login, `/` renders the shell; Logout returns to `/login`.
  </acceptance_criteria>
  <done>Unauthenticated requests redirect to /login; authenticated users see the shell; Logout clears the session — gating holds at both the proxy and the action/render layer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The Walking Skeleton: Next 16 app running locally on PGlite, password gate (proxy.ts + jose cookie), proven SELECT 1 round-trip, app shell with Logout.</what-built>
  <how-to-verify>
    1. Run `npm run dev` (use the recorded dev script — Turbopack or `--webpack`). App starts with no cloud dependency.
    2. Visit `http://localhost:3000/` → you are redirected to `/login`.
    3. Enter a WRONG password → inline "Incorrect password" error, you stay on `/login`.
    4. Enter the correct `APP_PASSWORD` (from `.env.local`) → you land on the app shell.
    5. Confirm the DB spike: the spike returns value `1` (via the temporary trigger from Task 1, or `npm run db:migrate:local` smoke if wired) — proving a live PGlite round-trip.
    6. Click Logout → you return to `/login`; revisiting `/` redirects to `/login` again.
  </how-to-verify>
  <resume-signal>Type "approved" if the gate, the redirect, and the DB round-trip all work, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → proxy.ts | Untrusted request; only a valid signed cookie may pass the gate |
| browser → Server Actions | Untrusted form input (password); validated + re-authorized server-side |
| app process → env vars | APP_PASSWORD / SESSION_SECRET are server-only secrets; must never reach the client bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | session cookie | mitigate | Cookie is a `jose` HS256-signed JWT (not the password, not unsigned); `httpOnly`+`secure`+`sameSite=lax`. Forged/edited cookies fail `verifySession`. |
| T-01-02 | Elevation of Privilege | proxy.ts gate (CVE-2025-29927 `x-middleware-subrequest`) | mitigate | Next 16.2.7 is patched; AND every protected Server Component/Action re-verifies the cookie — proxy.ts is treated as UX, not the boundary. |
| T-01-03 | Information Disclosure | APP_PASSWORD / SESSION_SECRET in client bundle | mitigate | Server-only env vars; `.env.local` gitignored; explicit grep gate that no `NEXT_PUBLIC_` prefixes either secret; `.env.example` warns. |
| T-01-04 | Information Disclosure | password compare timing side-channel | mitigate | `node:crypto.timingSafeEqual` with a length guard, never `===`. |
| T-01-05 | Tampering | weak/short SESSION_SECRET → forgeable JWT | mitigate | SESSION_SECRET ≥32 bytes (documented in `.env.example`); HS256 over that key. |
| T-01-SC | Tampering | npm installs (pglite, postgres, jose, drizzle) | mitigate | RESEARCH Package Legitimacy Audit: all Approved (official repos, no postinstall). No `[ASSUMED]`/`[SUS]` package gates this plan blocks on. |
</threat_model>

<verification>
- `npm run build` compiles with no type errors.
- Unauthenticated GET `/` → 302 `/login`; correct password → cookie set + shell; wrong password → inline error, no cookie; Logout → cookie cleared.
- `dbSpike()` returns `{ ok: true, value: 1 }` against PGlite under the running dev server.
- Repo grep confirms no `NEXT_PUBLIC_APP_PASSWORD` / `NEXT_PUBLIC_SESSION_SECRET`.
</verification>

<success_criteria>
- ACCESS-01: correct password → persistent signed-cookie session (30-day sliding); Logout clears it.
- ACCESS-02: every route except `/login` + static is gated by proxy.ts AND re-checked per render/action.
- ROADMAP success criterion #6 (partial): the app runs fully on a local machine on PGlite with no cloud dependency; Supabase is a documented `DATABASE_URL` swap.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done. Record the working dev incantation (Turbopack vs `--webpack`) and the dual-driver seam contract for Plan 02.
</output>
