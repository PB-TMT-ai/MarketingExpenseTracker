---
phase: 03_1-actuals-grid-refinements
plan: 05
type: execute
wave: 3
depends_on: ["03_1-03", "03_1-04"]
files_modified:
  - app/(app)/actuals/offplan-modal.tsx
  - app/(app)/actuals/actuals-grid.tsx
  - lib/actuals/rows.ts
  - lib/actuals/colDefs.ts
  - lib/actuals/rows.test.ts
  - e2e/actuals.spec.ts
autonomous: false
requirements: [COMP-04]
must_haves:
  truths:
    - "A '+ off-plan execution' button opens a modal that collects SFID, dealer, who/where, reason, and the activity's actual fields"
    - "Submitting the modal calls addOffPlanExecution and the new exception row appears in the grid after refresh"
    - "Exception rows are visually distinguishable from plan-uploaded rows (an exception pill on the SFID cell)"
    - "A dupe-SFID exception surfaces the server's clean message in the modal (no crash)"
  artifacts:
    - path: "app/(app)/actuals/offplan-modal.tsx"
      provides: "off-plan exception modal mirroring pop-modal.tsx structure"
      contains: "addOffPlanExecution"
    - path: "lib/actuals/rows.ts"
      provides: "source surfaced in planContext (source: pr.source)"
      contains: "source"
    - path: "lib/actuals/colDefs.ts"
      provides: "SFID plan-column cellRenderer exception pill when plan.source==='exception'"
      contains: "exception"
  key_links:
    - from: "offplan-modal submit"
      to: "addOffPlanExecution action"
      via: "form submit → action → router.refresh"
      pattern: "addOffPlanExecution"
    - from: "colDefs SFID cellRenderer"
      to: "p.data.plan.source"
      via: "exception pill when source==='exception'"
      pattern: "source"
---

<objective>
Complete COMP-04: the off-plan-exception FRONTEND — the "+ off-plan execution" affordance, the entry modal, the exception pill, and the e2e proof. This depends on Plan 03 (the `addOffPlanExecution` action) and Plan 04 (the refactored `actuals-grid.tsx` where the button wires in).

This plan delivers:
- `offplan-modal.tsx` — a modal MIRRORING `pop-modal.tsx`'s overlay/panel structure, brand tokens, and button styles (no net-new visual design — ui_consistency_directive). It collects Identity (SFID, Dealer, Region/State/District/Taluka/Distributor), Audit (Reason, required), and the activity's actual fields driven by the registry (so a 7th activity needs no modal change — ACTV-03). Default status "In Progress" (D3.1-03). On submit it calls `addOffPlanExecution`, on success closes + `router.refresh()`, on dupe-SFID surfaces the server's clean message.
- A "+ off-plan execution" button in the actuals page header/grid (a client island) that opens the modal.
- `source: pr.source` added to `buildRowModel`'s planContext (rows.ts) so the grid can see exception rows.
- An SFID plan-column `cellRenderer` in colDefs.ts that appends an "exception" pill when `plan.source === 'exception'` (in-system badge token, a distinct violet/rose pill per the directive).
- e2e for the off-plan affordance.

Purpose: This is the user-facing path that satisfies COMP-04 success criterion 4 (a deliberate, audited off-plan path) while preserving COMP-01 (the structural guard is untouched — the server inserts the plan_row first then FKs the execution).

Note: COMP-04 spans Plan 03 (backend) and Plan 05 (frontend); together they fully satisfy the requirement.

Output: `offplan-modal.tsx`; button wiring in `actuals-grid.tsx`; `source` in planContext (rows.ts) + a test; exception pill in colDefs.ts; e2e in actuals.spec.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03_1-actuals-grid-refinements/03_1-CONTEXT.md
@.planning/phases/03_1-actuals-grid-refinements/03_1-RESEARCH.md
@.planning/phases/03_1-actuals-grid-refinements/03_1-03-SUMMARY.md
@.planning/phases/03_1-actuals-grid-refinements/03_1-04-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs — from the codebase + Plans 03/04. -->

From lib/actions/executions.ts (Plan 03) — the action this modal calls:
```typescript
// addOffPlanExecution(prev, input): Promise<AddOffPlanState>
// input: { periodId, activity, sfid, dealer, region?, state?, district?, taluka?, distributor?, exceptionReason, fields: Record<string,unknown> }
// returns { ok:true, planRowId, executionId } | { ok:false, error }  // dupe-SFID 23505 → clean error
```

From app/(app)/actuals/pop-modal.tsx — the modal STRUCTURE to mirror (ui_consistency_directive — reuse these exact patterns):
```typescript
// Overlay: <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4" data-slot="..." role="dialog" aria-modal="true" onClick={onClose}>
// Panel: <div className="max-h-[85vh] w-full max-w-2xl overflow-auto rounded-xl border border-neutral-200 bg-white shadow-xl" onClick={(e)=>e.stopPropagation()}>
// Header with title + ✕ close; footer with Cancel (border) + primary (bg-neutral-900 text-white) buttons; min-h-11 tap targets.
// Inputs: rounded border border-neutral-300 px-2 py-1 text-sm. select for enum/status from enumValues.
```

From lib/activities/registry.ts + counter-wall.ts — registry-driven fields (iterate to render inputs; ACTV-03):
```typescript
getActivity(key): ActivityConfig | undefined;  // .actualColumns: FieldDef[] (key,label,kind,enumValues?,computeFrom?)
// FieldDef.kind ∈ text|number|currency|date|status|enum|lat|long. status enumValues e.g. ["Pending","In Progress","Done"].
// Render: status/enum → <select> from enumValues (default "In Progress"); number/currency → number input; date → DD/MM/YY text; lat/long/text → text.
```

From lib/actuals/rows.ts — buildRowModel planContext (Plan 01 added PlanRowRecord.source; ADD source to planContext here):
```typescript
const planContext = { sfid: pr.sfid, region: pr.region, ..., plannedCost: pr.plannedCost, ...pr.fields }; // ADD: source: pr.source
```

From lib/actuals/colDefs.ts — the SFID plan column is built in the planCols map (`field: plan.${f.key}`); add a cellRenderer to the SFID column:
```typescript
// plan cols: { headerName, field: `plan.${f.key}`, editable:false, cellClass:"ag-cell-plan", minWidth }
// For the sfid column (f.key === "sfid"): add cellRenderer that reads p.data?.plan?.source and appends an "exception" pill when === 'exception'.
```

Existing in-system pill pattern (page.tsx): `inline-flex items-center rounded-full bg-amber-50 px-2 py-0.5 text-xs font-medium text-amber-800`. For "exception" use a DISTINCT in-system color (violet/rose), e.g. `bg-violet-50 text-violet-800` (ui_consistency_directive — distinct but in-system).

From app/(app)/actuals/actuals-grid.tsx (Plan 04) — where to wire the button + render the modal (client component; has activityKey, periodId, apiRef; renders FilterBar + bars + grid). router via `useRouter` from next/navigation for refresh.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: source in planContext (rows.ts) + exception pill cellRenderer (colDefs.ts) + rows.test.ts assertion</name>
  <read_first>
    - lib/actuals/rows.ts (buildRowModel planContext object ~line 130-141)
    - lib/actuals/colDefs.ts (the planCols map ~line 76-82 — where to attach a cellRenderer to the sfid column)
    - lib/actuals/rows.test.ts (the plan-context describe block — add a source assertion; makePlanRow fixture now needs source)
    - app/(app)/actuals/page.tsx (the in-system pill class pattern: rounded-full bg-*-50 px-2 py-0.5 text-xs ...)
    - .planning/phases/03_1-actuals-grid-refinements/03_1-RESEARCH.md §COMP-04 "Grid surfacing" + ui_consistency_directive
  </read_first>
  <action>
    In rows.ts buildRowModel, add `source: pr.source` to the planContext object (PlanRowRecord.source exists from Plan 01). This surfaces the exception flag on each row via `plan.source`.
    In colDefs.ts, for the SFID plan column (the planColumns entry with `f.key === "sfid"`), add a `cellRenderer` that renders the SFID value and, when `p.data?.plan?.source === 'exception'`, appends an in-system "exception" pill (distinct violet/rose token, e.g. `inline-flex items-center rounded-full bg-violet-50 px-2 py-0.5 text-xs font-medium text-violet-800`, matching the page.tsx pill pattern). Keep the column read-only (editable:false) and presentational — the renderer only displays; it never makes the cell editable or introduces an SFID. Other plan columns are unchanged.
    In rows.test.ts: add `source: "plan-upload"` to the makePlanRow fixture (the type now requires it), and add an assertion in the plan-context describe block that `rows[0].plan["source"]` equals the plan row's source. Add one case asserting a `source: "exception"` plan row surfaces `plan.source === "exception"`.
  </action>
  <verify>
    <automated>npx vitest run lib/actuals/rows.test.ts lib/actuals/colDefs.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>
    rows.ts planContext includes `source: pr.source`. colDefs.ts SFID plan column has a cellRenderer that shows an in-system exception pill when plan.source==='exception' (read-only, presentational). rows.test.ts asserts plan.source flows through for both 'plan-upload' and 'exception'. tests + tsc pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: offplan-modal.tsx (mirror pop-modal.tsx) + "+ off-plan execution" button wiring</name>
  <read_first>
    - app/(app)/actuals/pop-modal.tsx (FULL — the overlay/panel/header/footer structure + Tailwind tokens to mirror; data-slot conventions)
    - app/(app)/actuals/actuals-grid.tsx (Plan 04 refactor — where to add the button + modal state + useRouter; activityKey/periodId in scope)
    - lib/actions/executions.ts (addOffPlanExecution signature + AddOffPlanState from Plan 03)
    - lib/activities/registry.ts + counter-wall.ts (getActivity, actualColumns, FieldDef.kind/enumValues — drives the field inputs)
    - .planning/phases/03_1-actuals-grid-refinements/03_1-RESEARCH.md §COMP-04 "UI affordance" + D3.1-02/03/08 + ui_consistency_directive
  </read_first>
  <action>
    Create `app/(app)/actuals/offplan-modal.tsx` (client component) MIRRORING pop-modal.tsx's overlay/panel/header/footer markup and Tailwind tokens (fixed inset-0 z-50 overlay, max-w-2xl rounded-xl panel, ✕ close, Cancel + primary buttons, min-h-11 tap targets, data-slot="offplan-modal"). Props: `{ activityKey, periodId, onClose, onSuccess }`. Fields: Identity — SFID (required text), Dealer (required text), Region/State/District/Taluka/Distributor (optional free-text → the real plan_rows who/where columns). Audit — Reason (required textarea → exceptionReason). Actuals — iterate `getActivity(activityKey).actualColumns` and render one input per FieldDef: status/enum → <select> from enumValues defaulting to "In Progress" (D3.1-03); number/currency → number input; date → DD/MM/YY text input; lat/long/text → text input. This reuses the registry exactly like the grid (ACTV-03 — a 7th activity needs no modal change). On submit: assemble `fields` from the activity inputs and call `addOffPlanExecution(undefined, { periodId, activity: activityKey, sfid, dealer, region?, state?, district?, taluka?, distributor?, exceptionReason, fields })` (via useActionState or a transition). On `{ok:true}` → call onSuccess (close + refresh). On `{ok:false}` → show the server's `error` inline in the modal (covers the dupe-SFID clean message — never crash). Disable submit while pending and when SFID/Dealer/Reason are empty.
    In actuals-grid.tsx (Plan 04 grid): add `const [offPlanOpen, setOffPlanOpen] = useState(false)` and a "+ off-plan execution" button in the grid header area (ABOVE the grid, e.g. near FilterBar — NOT inline in an AG Grid row toolbar, per D3.1-02). Render `<OffPlanModal>` when open, passing activityKey/periodId, `onClose={()=>setOffPlanOpen(false)}`, and `onSuccess` = close + `router.refresh()` (useRouter from next/navigation) so the new exception row appears (the action already revalidatePath'd /actuals). data-slot the button (e.g. `data-slot="offplan-open"`) for e2e.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint "app/(app)/actuals/offplan-modal.tsx" "app/(app)/actuals/actuals-grid.tsx"</automated>
  </verify>
  <done>
    offplan-modal.tsx mirrors pop-modal.tsx structure/tokens; collects identity + reason (required) + registry-driven activity fields (status defaults "In Progress"); calls addOffPlanExecution; shows the server error inline on dupe/failure; closes + refreshes on success. actuals-grid.tsx has a "+ off-plan execution" button (above the grid) opening the modal with router.refresh() onSuccess. tsc + eslint pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify off-plan modal creates an exception row with pill, blocks empty reason/SFID, surfaces dupe cleanly</name>
  <action>PAUSE for the user to run the dev server and manually verify the off-plan exception affordance per the steps below; do not proceed until they respond to the resume signal.</action>
  <what-built>
    The off-plan-exception affordance: a "+ off-plan execution" button opens a modal (matching the POP modal's look) where you enter an SFID not in the plan, the dealer, who/where, a required reason, and the activity's execution fields. Submitting creates ONE exception plan_row + the execution in one transaction and the new row appears in the grid with an "exception" pill on its SFID.
  </what-built>
  <how-to-verify>
    1. `npm run dev`, open `/actuals?activity=counter-wall` on a period that has a plan.
    2. Click "+ off-plan execution" — confirm the modal opens with the POP-modal look (overlay, panel, ✕ close, Cancel/primary buttons) and shows SFID, Dealer, who/where, Reason, and the counter-wall actual fields (Status defaulting to "In Progress").
    3. Try submitting with an empty Reason or SFID — confirm submit is disabled / blocked.
    4. Enter a NEW SFID (not in the plan), a dealer, a reason, and some actuals; submit — confirm the modal closes and a new row appears in the grid with an "exception" pill on its SFID cell.
    5. Open the modal again and enter an SFID that ALREADY exists in the plan; submit — confirm a clean inline message appears ("...already exists... use + add unit...") and nothing crashes.
    6. (Optional) Re-upload the plan for this activity WITHOUT the exception SFID (in /plans) and confirm the exception row still survives in /actuals (R4 guard from Plan 03).
  </how-to-verify>
  <resume-signal>Type "approved" if the modal creates an exception row with a pill, blocks empty reason/SFID, and surfaces the dupe message cleanly — or describe the issue.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: e2e — off-plan exception affordance</name>
  <read_first>
    - e2e/actuals.spec.ts (FULL — fixtures, how a period+plan is seeded, existing selectors, the activity selector)
    - app/(app)/actuals/offplan-modal.tsx + actuals-grid.tsx (data-slots: offplan-open, offplan-modal, the field inputs)
    - .planning/phases/03_1-actuals-grid-refinements/03_1-RESEARCH.md §COMP-04 + §Validation Architecture sampling dim 3
  </read_first>
  <action>
    Append an e2e test to actuals.spec.ts (do not rewrite existing tests). Happy path: open `/actuals` on a seeded period+plan, click `[data-slot="offplan-open"]`, assert `[data-slot="offplan-modal"]` is visible, fill SFID (a new value not in the plan), Dealer, Reason, and a status, submit, and assert the new exception row appears in the grid with the exception pill on its SFID cell (assert the pill element/text). If feasible within the harness, add a dupe-SFID assertion: enter an SFID already present and assert the inline error message shows and no new row is added. Reuse the existing fixture/seeding approach; use the activity selector to land on a measurement activity.
  </action>
  <verify>
    <automated>npx playwright test e2e/actuals.spec.ts</automated>
  </verify>
  <done>
    e2e/actuals.spec.ts has an off-plan test: open modal → fill identity+reason+actuals → submit → new exception row visible with the exception pill. `npx playwright test e2e/actuals.spec.ts` passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| modal → addOffPlanExecution | Untrusted exception input crosses to the server action (server re-validates; client modal is convenience only) |
| exception pill render | Read-only display of plan.source; must not become an edit/write path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03_1-14 | Tampering | off-plan guard via modal | mitigate | The modal only calls addOffPlanExecution; the server (Plan 03) does requireSession + Zod + inserts plan_row-then-execution. The client never writes the DB or an sfid to executions (COMP-01 preserved) |
| T-03_1-15 | Information Disclosure | dupe-SFID error surface | mitigate | The server returns a clean {ok:false} message; the modal renders it inline (no stack/500 leaked to the user) |
| T-03_1-16 | Tampering | exception pill cellRenderer | accept | Pure read-only display of plan.source; no input crosses here (low risk) |
| T-03_1-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — React + next/navigation + existing action |
</threat_model>

<verification>
- `npx tsc --noEmit` + eslint pass on offplan-modal.tsx, actuals-grid.tsx.
- `npx vitest run lib/actuals/rows.test.ts lib/actuals/colDefs.test.ts` pass (source in planContext + pill).
- `npx playwright test e2e/actuals.spec.ts` passes (off-plan affordance).
- Manual checkpoint confirms the modal creates an exception row + pill, blocks empty reason/SFID, surfaces dupe cleanly.
- Full suite green: `npm test` + `npm run e2e` (phase gate).
</verification>

<success_criteria>
- "+ off-plan execution" button opens a modal mirroring pop-modal.tsx that collects SFID, dealer, who/where, required reason, and registry-driven activity fields (status default "In Progress").
- Submit calls addOffPlanExecution; success closes + refreshes and the exception row appears; dupe-SFID shows the server's clean message.
- Exception rows carry `plan.source` and render an in-system exception pill on the SFID cell.
- COMP-04 fully satisfied across Plan 03 (backend) + Plan 05 (frontend); COMP-01 structural guard unchanged.
</success_criteria>

<output>
Create `.planning/phases/03_1-actuals-grid-refinements/03_1-05-SUMMARY.md` when done. Confirm COMP-04 is fully delivered (backend Plan 03 + frontend Plan 05) and note the D3.1-08 audit limitation (no createdBy) for a future auth phase.
</output>
