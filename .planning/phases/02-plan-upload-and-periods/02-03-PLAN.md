---
phase: 02-plan-upload-and-periods
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - app/(app)/plans/page.tsx
  - app/(app)/plans/upload/page.tsx
  - app/(app)/plans/upload/upload-form.tsx
  - app/(app)/plans/upload/preview-table.tsx
  - app/(app)/plans/upload/template-button.tsx
  - app/(app)/layout.tsx
  - e2e/plans.spec.ts
  - e2e/fixtures/plan-counter-wall.xlsx
autonomous: true
requirements: [PLAN-01, PLAN-02, PLAN-03, COMP-02]

must_haves:
  truths:
    - "A user can navigate from the (app) shell header to /plans and see a list grouped by (activity × period) showing the existing plan-row count per cell"
    - "A user can click 'Download template' on the upload page for any of the six activities and receive marketing-plan-template-{activity}.xlsx whose header row equals planColumns labels in order"
    - "Selecting an activity + period and choosing a file triggers CLIENT-SIDE parse + header validation + preview classification BEFORE any Server Action runs (D2-06: server never sees an .xlsx)"
    - "The preview table groups rows by classification (errors first, then duplicates, then updates, then valids) with per-row counts visible at the top"
    - "Clicking 'Commit upload' posts the already-parsed valid/update rows (NOT the file) to commitPlanUpload and on success shows the inserted/updated/deleted counts and revalidates /plans"
    - "On FK-blocked commit, the preview UI lists the blockedDealers (sfid + executionCount) returned by the Server Action; the user is told to fix the source Excel or retire actuals before retrying (COMP-02 surface)"
    - "Period selector defaults to the active period (D-11) so the common case is one click + a file"
    - "An e2e Playwright test proves the happy path (upload → preview → commit → /plans list shows the rows) AND the blocked-by-actuals path (upload → record execution → re-upload missing SFID → blocked list shown, no destructive write)"
  artifacts:
    - path: "app/(app)/plans/page.tsx"
      provides: "Server Component listing plan_rows grouped by (activity × period); each cell links to /plans/upload?activity=X&periodId=Y"
      contains: "force-dynamic"
      min_lines: 25
    - path: "app/(app)/plans/upload/page.tsx"
      provides: "Server Component shell hosting the client upload form; reads listPeriods + getActivePeriod for the form's defaults"
      contains: "UploadForm"
    - path: "app/(app)/plans/upload/upload-form.tsx"
      provides: "Client Component: activity+period picker, file input, client-side parse → preview → commit"
      contains: "use client"
      min_lines: 60
    - path: "app/(app)/plans/upload/preview-table.tsx"
      provides: "Client Component renders PreviewRow[] grouped by classification with per-class counts"
      contains: "use client"
    - path: "app/(app)/plans/upload/template-button.tsx"
      provides: "Client Component: browser-side template download via SheetJS write + Blob URL"
      contains: "use client"
    - path: "e2e/plans.spec.ts"
      provides: "Playwright: happy-path upload + blocked-by-actuals scenario"
      contains: "blockedDealers|blocked"
      min_lines: 50
    - path: "e2e/fixtures/plan-counter-wall.xlsx"
      provides: "Pre-built fixture: 2 counter-wall rows (SF-A, SF-B) with valid shared + jsonb data"
  key_links:
    - from: "app/(app)/plans/upload/upload-form.tsx"
      to: "lib/excel/parse.ts + validate.ts (Plan 02-01)"
      via: "client-side readWorkbook + buildPreview before any Server Action call"
      pattern: "readWorkbook|buildPreview"
    - from: "app/(app)/plans/upload/upload-form.tsx"
      to: "lib/actions/plans.ts (Plan 02-02)"
      via: "commitPlanUpload called with ALREADY-PARSED valid+update rows (never the File)"
      pattern: "commitPlanUpload"
    - from: "app/(app)/plans/upload/template-button.tsx"
      to: "lib/excel/template.ts (Plan 02-01)"
      via: "buildPlanTemplate(activityKey) + Blob URL + <a download>"
      pattern: "buildPlanTemplate|TEMPLATE_FILE_NAME"
    - from: "app/(app)/layout.tsx"
      to: "/plans route"
      via: "header nav link added next to Periods + Items"
      pattern: "/plans"
    - from: "app/(app)/plans/page.tsx"
      to: "lib/db/plan-rows.ts (Plan 02-02)"
      via: "listByPeriodActivity or an aggregate query to show count per (activity × period)"
      pattern: "planRows|plan_rows"
---

<objective>
Ship the user-facing surface for Phase 2: a `/plans` list page, the upload form with client-side parse + preview + commit, the per-activity template-download button, the shell nav link, and a Playwright e2e covering both the happy path AND the load-bearing blocked-by-actuals path that closes COMP-02. Every Excel operation runs in the browser (D2-06) — the server only ever sees the already-parsed JSON rows that Plan 02-02's `commitPlanUpload` validates with `PLAN_ROW_SCHEMAS[activity]`.

Purpose: Closes PLAN-01 (template), PLAN-02 (header validate UI), PLAN-03 (preview UI), and COMP-02 (off-plan / rejection surface — transient UI per D2-02; the actuals side of COMP-02 lands in Phase 3). After this plan, the JSW team can upload an approved plan end-to-end and trust the off-plan guard structurally.
Output: 5 UI files under `app/(app)/plans/`, a one-line header-nav edit in `app/(app)/layout.tsx`, an e2e spec with a fixture .xlsx, and ZERO new dependencies (SheetJS already installed in 02-01; AG Grid is a Phase 3 concern).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-plan-upload-and-periods/02-CONTEXT.md
@.planning/phases/02-plan-upload-and-periods/02-RESEARCH.md
@.planning/phases/02-plan-upload-and-periods/02-PATTERNS.md
@.planning/phases/02-plan-upload-and-periods/02-01-PLAN.md
@.planning/phases/02-plan-upload-and-periods/02-02-PLAN.md

# Existing seams this plan builds against:
@app/(app)/layout.tsx
@app/(app)/periods/page.tsx
@app/(app)/periods/period-form.tsx
@app/(app)/items/page.tsx
@app/(app)/items/item-form.tsx
@lib/periods/active.ts
@lib/db/periods.ts
@lib/activities/registry.ts
@lib/activities/types.ts
@lib/excel/parse.ts
@lib/excel/template.ts
@lib/excel/validate.ts
@lib/excel/types.ts
@lib/actions/plans.ts
@lib/db/plan-rows.ts
@e2e/periods.spec.ts

<interfaces>
<!-- Contracts the executor needs — extracted from the live codebase + sibling plans. Use these directly. -->

From lib/activities/registry.ts:
```
export const ACTIVITIES: Readonly<Record<ActivityKey, ActivityConfig>>;
export const ACTIVITY_KEYS: readonly ActivityKey[];
export function getActivity(key: string): ActivityConfig | undefined;
```

From lib/periods/active.ts (the PRD-02 scoping seam Plan 01-04 shipped):
```
export async function getActivePeriod(): Promise<PeriodRow | null>;
```

From lib/db/periods.ts:
```
export async function listPeriods(): Promise<PeriodRow[]>;
export type PeriodRow = { id, type, label, startDate, endDate, isActive, createdAt };
```

From lib/db/plan-rows.ts (Plan 02-02):
```
export async function listByPeriodActivity(periodId, activity): Promise<PlanRowRecord[]>;
// For the (activity × period) grid count, either reuse this or add an aggregate `countByPeriodActivity()` (preferred — one query for the whole grid).
```

From lib/actions/plans.ts (Plan 02-02 — the action commit form calls):
```
export type CommitPlanState =
  | { ok: true; inserted: number; updated: number; deleted: number }
  | { ok: false; error: string; blockedDealers?: { sfid: string; executionCount: number }[] };
export async function commitPlanUpload(
  prev: unknown,
  input: { periodId: number; activity: ActivityKey; rows: ParsedRow[] },
): Promise<CommitPlanState>;
```

From lib/excel/parse.ts + template.ts + validate.ts + types.ts (Plan 02-01):
```
import * as XLSX from "xlsx";
export function readWorkbook(buf: ArrayBuffer): unknown[][];
export function coerceCell(raw, field): { ok: true; value } | { ok: false; error };
export function buildPlanTemplate(activityKey: ActivityKey): ArrayBuffer;
export function TEMPLATE_FILE_NAME(activityKey: ActivityKey): string;  // "marketing-plan-template-{key}.xlsx"
export function validateHeaders(headerRow, planColumns): HeaderError | null;
export function buildPreview(rows, planColumns, coerce): PreviewRow[];
export type ParsedRow = { sfid, sharedFields, jsonbFields, plannedCost };
export type PreviewRow = { rowNumber, classification, sfid, parsed, errors };
```

From app/(app)/periods/period-form.tsx (the useActionState client-form pattern to mirror — L1–17):
```
"use client";
import { useActionState, useEffect, useRef } from "react";
const [state, formAction, pending] = useActionState<CommitPlanState, FormData>(commitPlanUploadForm, {});
useEffect(() => { if (state.ok) formRef.current?.reset(); }, [state]);
// inline error: <p role="alert" className="text-sm text-red-600">{state.error}</p>
```

From app/(app)/periods/page.tsx (Server Component card + list shape to mirror — L14–35):
```
<div className="mx-auto grid max-w-3xl gap-6">
  <header><h1 className="text-xl font-semibold">...</h1><p className="mt-1 text-sm text-neutral-600">...</p></header>
  <section className="rounded-xl border border-neutral-200 bg-white shadow-sm">
    <h2 className="border-b border-neutral-200 p-4 text-base font-semibold">...</h2>
    {rows.length === 0 ? (<p className="p-6 text-sm text-neutral-500">No ...</p>)
      : (<ul data-slot="plan-list" className="divide-y divide-neutral-200">...</ul>)}
  </section>
</div>
// data-slot is the e2e selector contract — give every list/marker a stable data-slot.
```

From app/(app)/layout.tsx L34–46 (the nav strip Plan 02-03 edits):
```
<nav className="flex items-center gap-2 text-sm">
  <Link href="/periods" ...>Periods</Link>
  <Link href="/items" ...>Items</Link>
  // Add: <Link href="/plans" ...>Plans</Link>  (between Items and the logout form, or before Items — pick a deterministic spot)
  <form action={logout}>...</form>
</nav>
```

From e2e/periods.spec.ts (the Playwright pattern — L1–16 verbatim for login):
```
const DEV_PASSWORD = "jsw-marketing-2026";
async function login(page) {
  await page.context().clearCookies();
  await page.goto("/login");
  await page.fill("input[name=password]", DEV_PASSWORD);
  await page.click("button[type=submit]");
  await page.waitForURL("/", { timeout: 10_000 });
}
// File upload:  await page.setInputFiles("input[type=file]", "e2e/fixtures/plan-counter-wall.xlsx");
// Network settle: await page.waitForLoadState("networkidle");
// Action errors: await expect(page.getByRole("alert")).toBeVisible();
```

Template-download snippet (RESEARCH §2 — copy verbatim into template-button.tsx):
```
const out = buildPlanTemplate(activityKey);          // ArrayBuffer
const blob = new Blob([out], { type: "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" });
const url = URL.createObjectURL(blob);
const a = document.createElement("a");
a.href = url;
a.download = TEMPLATE_FILE_NAME(activityKey);
document.body.appendChild(a);
a.click();
document.body.removeChild(a);
URL.revokeObjectURL(url);
```

Upload-form skeleton (RESEARCH §6 — adapt to use the commitPlanUploadForm wrapper from Plan 02-02 + useActionState; pass `rows` as a hidden JSON input so the FormData contract matches the established period-form shape):
```
"use client";
import { useActionState, useEffect, useRef, useState } from "react";
const [state, formAction, pending] = useActionState<CommitPlanState, FormData>(commitPlanUploadForm, {});
// file → arrayBuffer (NOT FileReader — native promise)
const buf = await file.arrayBuffer();
const rows = readWorkbook(buf);
const headerErr = validateHeaders(rows[0] ?? [], ACTIVITIES[activity].planColumns);
if (headerErr) { setParseError(...); return; }
const preview = buildPreview(rows, ACTIVITIES[activity].planColumns, coerceCell);
// On commit: serialize valid+update rows to <input type="hidden" name="rows" value={JSON.stringify(toCommit)}>
// AND <input type="hidden" name="periodId" value={periodId}> + <input type="hidden" name="activity" value={activity}>.
```
</interfaces>

<scope_guard>
- DO NOT install any new package. SheetJS is already in package.json from Plan 02-01. AG Grid is a Phase 3 concern. NO shadcn (Tailwind + native elements only — matches periods/items pages).
- DO NOT add a Route Handler. Template download is a browser-side click handler (RESEARCH §2 + §7). The commit posts via a Server Action wrapper (commitPlanUploadForm from Plan 02-02).
- DO NOT parse `.xlsx` on the server. NO `import * as XLSX from "xlsx"` anywhere outside `app/(app)/plans/upload/*.tsx` (Client Components). Acceptance gate enforces this (D2-06 + RESEARCH "Common Pitfalls" B).
- DO NOT change `lib/db/schema.ts`, `lib/actions/plans.ts`, `lib/db/plan-rows.ts`, or `lib/excel/*`. Those are owned by Plans 02-01 / 02-02. If you discover a missing helper in `lib/db/plan-rows.ts` (e.g. an aggregate count for the grid), add a minimal new function — DO NOT rewrite existing exports.
- DO NOT persist off-plan rejections to a new table (D2-02). The preview UI is the only surface for rejections (transient).
- DO NOT pre-fill the template from a prior period (D2-05 — headers only).
- DO NOT add an `import_rejections` table or audit log (deferred per D2-02).
- DO NOT add Web Worker offload, virtualisation, or paging in v1 — raw HTML preview is the discretion-zone choice; revisit only if a real plan exceeds 5k rows (CONTEXT line 45).
- POP / dealer-cert / GSB / NLB upload paths MUST work without a planned-cost cell in the .xlsx. The Server Action in 02-02 already persists `plannedCost = null` for those activities; the UI must NOT validate-require planned-cost when the activity's planColumns lacks it (just iterate the activity's planColumns).
- DO NOT add a Phase-3 actuals UI. The blocked-by-actuals e2e test seeds the execution via a database-level helper or the smoke harness, NOT via a UI flow that doesn't exist yet.
</scope_guard>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /plans list page + template-button + shell nav link</name>
  <files>app/(app)/plans/page.tsx, app/(app)/plans/upload/template-button.tsx, app/(app)/layout.tsx</files>
  <read_first>
    - app/(app)/periods/page.tsx (the Server Component card+list shape + data-slot pattern to mirror)
    - app/(app)/items/page.tsx (a second card+list example)
    - app/(app)/layout.tsx (the nav strip to extend; the existing Periods + Items links)
    - lib/db/plan-rows.ts (Plan 02-02 — listByPeriodActivity; if no aggregate count helper exists, add one minimal `countByPeriodActivity(): Promise<{ periodId, activity, count }[]>` and re-export it — DO NOT rewrite existing exports)
    - lib/db/periods.ts (listPeriods)
    - lib/activities/registry.ts (ACTIVITIES, ACTIVITY_KEYS — iterate for the grid)
    - lib/excel/template.ts (Plan 02-01 — buildPlanTemplate, TEMPLATE_FILE_NAME)
    - .planning/phases/02-plan-upload-and-periods/02-RESEARCH.md §2 + §7 (browser-side template download snippet)
  </read_first>
  <action>
    Create `app/(app)/plans/page.tsx` — Server Component with `export const dynamic = "force-dynamic"`. Reads `listPeriods()` + iterates `ACTIVITY_KEYS` × periods to render a grid of cards (one per `(activity, period)` cell). Each cell shows the activity label + period label + count of existing plan_rows; if count > 0 the cell links to `/plans/upload?activity={key}&periodId={id}` (re-upload mode) and shows "N rows · Re-upload"; if count === 0 it links to the same URL but shows "Empty · Upload". Use the periods/page.tsx outer shell verbatim: `<div className="mx-auto grid max-w-5xl gap-6">` (wider than periods because of the grid), `<header>` with h1 + p, a single `<section className="rounded-xl border border-neutral-200 bg-white shadow-sm">` wrapping the grid. Use `data-slot="plan-grid"` on the grid container and `data-slot="plan-cell"` on each cell (e2e selectors). When NO periods exist, show "No periods yet — create one in /periods" with a link. Add a prominent "Upload a plan" button at the top right of the section that links to `/plans/upload` (no querystring — the form picks defaults). Implement `countByPeriodActivity()` in `lib/db/plan-rows.ts` if not already present: a single grouped SELECT `SELECT period_id, activity, count(*)::int FROM plan_rows GROUP BY period_id, activity`. The page assembles the count map and renders missing cells as 0.

    Create `app/(app)/plans/upload/template-button.tsx` as a `"use client"` component. Props: `{ activity: ActivityKey }`. On click runs the RESEARCH §2 snippet verbatim (adapted to use Plan 02-01's `buildPlanTemplate` + `TEMPLATE_FILE_NAME` — DO NOT re-import `xlsx` here, the template module owns SheetJS). Button styling matches the periods page's "Make active" button: `className="rounded-md border border-neutral-300 px-3 py-1.5 text-sm hover:bg-neutral-50"`. `data-slot="template-button"` on the button (e2e selector).

    Finally edit `app/(app)/layout.tsx` — add `<Link href="/plans" className="rounded-md border border-neutral-200 px-3 py-1.5 hover:bg-neutral-50">Plans</Link>` to the nav strip BETWEEN the Periods and Items links (deterministic order: Periods → Plans → Items → Logout). No other layout change.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` compiles with `/plans` and `/plans/upload` routes present in the (app) group route table.
    - `Select-String -Path "app/(app)/plans/page.tsx" -Pattern "force-dynamic"` matches AND `-Pattern "ACTIVITY_KEYS|listByPeriodActivity|countByPeriodActivity"` matches.
    - `Select-String -Path "app/(app)/plans/page.tsx" -Pattern "data-slot=\"plan-grid\"|data-slot=\"plan-cell\""` matches at least TWICE — the e2e selectors are wired.
    - `Select-String -Path "app/(app)/plans/upload/template-button.tsx" -Pattern "use client"` matches at line 1 AND `-Pattern "buildPlanTemplate|TEMPLATE_FILE_NAME"` matches AND `-Pattern "URL.createObjectURL"` matches.
    - `Select-String -Path "app/(app)/plans/upload/template-button.tsx" -Pattern "from \"xlsx\""` returns ZERO matches — template-button reuses Plan 02-01's helper; SheetJS coupling stays in lib/excel/.
    - `Select-String -Path "app/(app)/layout.tsx" -Pattern "href=\"/plans\""` matches AND the existing `/periods` + `/items` + `<form action={logout}>` lines are unchanged (no regressions in the existing nav).
    - `git diff package.json` is empty for `dependencies`/`devDependencies` (no install in this task).
  </acceptance_criteria>
  <done>Plans list page renders a (activity × period) grid with counts and links; template button downloads marketing-plan-template-{key}.xlsx via browser-side SheetJS write; the shell nav links to /plans.</done>
</task>

<task type="auto">
  <name>Task 2: Upload form (client parse → preview → commit) + preview-table component + upload page shell</name>
  <files>app/(app)/plans/upload/page.tsx, app/(app)/plans/upload/upload-form.tsx, app/(app)/plans/upload/preview-table.tsx</files>
  <read_first>
    - app/(app)/periods/period-form.tsx (the useActionState + formRef + inline-error pattern to mirror)
    - app/(app)/items/item-form.tsx (a second useActionState example)
    - lib/excel/parse.ts (Plan 02-01 — readWorkbook + coerceCell signatures)
    - lib/excel/validate.ts (Plan 02-01 — validateHeaders + buildPreview signatures + the PreviewRow shape)
    - lib/excel/types.ts (Plan 02-01 — Classification, PreviewRow, HeaderError, ParsedRow)
    - lib/actions/plans.ts (Plan 02-02 — commitPlanUpload + commitPlanUploadForm + CommitPlanState + blockedDealers shape)
    - lib/periods/active.ts (getActivePeriod — drives the period selector default per D-11 + CONTEXT discretion item)
    - lib/db/periods.ts (listPeriods — populates the period dropdown)
    - lib/activities/registry.ts (ACTIVITIES — populates the activity dropdown; the form iterates Object.values)
    - .planning/phases/02-plan-upload-and-periods/02-RESEARCH.md §6 (the upload-form skeleton — adapt to FormData + useActionState shape per PATTERNS.md L73)
  </read_first>
  <action>
    Create `app/(app)/plans/upload/page.tsx` — Server Component (`export const dynamic = "force-dynamic"`). Reads `listPeriods()` + `getActivePeriod()` + reads the `?activity` and `?periodId` querystring (Next 16 server-component searchParams pattern). Passes `{ periods, defaultActivity, defaultPeriodId }` as props to the Client `<UploadForm />`. Outer shell mirrors periods/page.tsx: `<div className="mx-auto grid max-w-4xl gap-6">` with a header + a single section wrapping the form.

    Create `app/(app)/plans/upload/upload-form.tsx` (`"use client"`). State: `activity` (ActivityKey, initial = defaultActivity ?? "counter-wall"), `periodId` (number, initial = defaultPeriodId ?? activePeriod.id), `preview` (PreviewRow[] | null), `parseError` (string | null). On file `<input>` change: (a) read `file.arrayBuffer()` (NOT FileReader — native Promise per RESEARCH §6); (b) call `readWorkbook(buf)` (wrapped in try/catch to surface parse errors as parseError); (c) call `validateHeaders(rows[0] ?? [], ACTIVITIES[activity].planColumns)` — if it returns a HeaderError, surface it as parseError with the missing/extra detail; (d) call `buildPreview(rows, ACTIVITIES[activity].planColumns, coerceCell)` and set `setPreview(...)`. Cap file size at 10 MB before `arrayBuffer()` (RESEARCH §6 — surface "File larger than 10 MB" as parseError). When activity changes via the selector, ALSO clear `preview` (the per-activity schema changed; previous preview is stale).

    Form submission uses `useActionState(commitPlanUploadForm, {})` from Plan 02-02 — but the form posts via a hidden JSON field. On the "Commit upload" submit: do NOT call commitPlanUpload directly — instead let `useActionState`'s form-action drive the POST, with the form containing three hidden inputs: `<input type="hidden" name="periodId" value={periodId} />`, `<input type="hidden" name="activity" value={activity} />`, `<input type="hidden" name="rows" value={JSON.stringify(toCommit)} />` where `toCommit = preview.filter(p => p.classification === "valid" || p.classification === "update").map(p => p.parsed)`. The commit button is disabled when `preview === null`, when no valid/update rows exist, or when `pending` is true. Activity + period selectors are inside the form so they post too (or stay outside if you prefer driving them as React state — your call, but the hidden inputs are the source of truth for the action).

    After commit: when `state.ok === true`, render a success block (e.g. "Inserted X · Updated Y · Deleted Z") and a "Back to /plans" link (the action already calls `revalidatePath("/plans")`). When `state.ok === false` AND `state.blockedDealers` is present, render the BlockedDealers list below the preview: `data-slot="blocked-dealers"` containing one entry per dealer with the SFID + execution count + the message "Fix the source Excel to include this SFID, or retire its actuals, then retry." (COMP-02 transient surface). When `state.ok === false` without blockedDealers, render a generic `<p role="alert">` with `state.error`.

    Create `app/(app)/plans/upload/preview-table.tsx` (`"use client"`). Props: `{ preview: PreviewRow[] }`. Compute counts per classification at the top: `{ fieldError: N, duplicate: N, update: N, valid: N }` rendered as colored pill badges. Render the table grouped by classification IN THIS ORDER: fieldError, duplicate, update, valid (CONTEXT line 119 — errors first so the user's eye lands on what to fix). Each row shows rowNumber (Excel row), sfid, classification badge, and for fieldErrors the per-cell `col + reason` from `row.errors[]`. Use a stable `data-slot="preview-row"` attribute on each row and `data-classification={row.classification}` so e2e can filter. Use Tailwind only; no virtualisation in v1 (CONTEXT line 45). The component is presentation-only — receives PreviewRow[], emits nothing back.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` compiles. The /plans/upload route is present in the (app) group route table.
    - `Select-String -Path "app/(app)/plans/upload/upload-form.tsx" -Pattern "use client"` matches at line 1.
    - `Select-String -Path "app/(app)/plans/upload/upload-form.tsx" -Pattern "readWorkbook"` matches AND `-Pattern "validateHeaders"` matches AND `-Pattern "buildPreview"` matches — the client-side pipeline is wired (D2-06).
    - `Select-String -Path "app/(app)/plans/upload/upload-form.tsx" -Pattern "file.arrayBuffer"` matches — native Promise API, not FileReader (RESEARCH §6).
    - `Select-String -Path "app/(app)/plans/upload/upload-form.tsx" -Pattern "useActionState"` matches AND `-Pattern "commitPlanUploadForm|commitPlanUpload"` matches.
    - `Select-String -Path "app/(app)/plans/upload/upload-form.tsx" -Pattern "type=\"hidden\".*name=\"rows\"|name=\"rows\".*type=\"hidden\""` matches OR an equivalent JSON-serialized hidden field — the parsed rows (NOT the File) are what gets posted.
    - `Select-String -Path "app/(app)/plans/upload/upload-form.tsx" -Pattern "data-slot=\"blocked-dealers\""` matches — the COMP-02 surface is wired.
    - `Select-String -Path "app/(app)/plans/upload/preview-table.tsx" -Pattern "data-slot=\"preview-row\""` matches AND `-Pattern "fieldError|duplicate|update|valid"` matches — the e2e selector contract and the four classifications are present.
    - `Select-String -Path "app/(app)/plans/upload/upload-form.tsx","app/(app)/plans/upload/preview-table.tsx","app/(app)/plans/upload/page.tsx" -Pattern "10 \\* 1024 \\* 1024|10_000_000|10485760"` matches at least once — the 10 MB cap is present.
    - `Select-String -Path "lib/db/**/*.ts","lib/actions/**/*.ts" -Pattern "import \\* as XLSX|from \"xlsx\""` returns ZERO matches outside `lib/excel/` — the server NEVER imports SheetJS (D2-06 / "Common Pitfalls" B).
    - `git diff package.json` is empty for `dependencies`/`devDependencies`.
  </acceptance_criteria>
  <done>Upload form parses + validates + previews client-side; commit posts parsed rows (NOT the .xlsx) to commitPlanUploadForm; success and FK-blocked states render the right counts/blockedDealers; preview-table groups by classification with the right ordering and e2e selectors.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright e2e — happy path + blocked-by-actuals (closes COMP-02 user-facing path)</name>
  <files>e2e/plans.spec.ts, e2e/fixtures/plan-counter-wall.xlsx, package.json</files>
  <read_first>
    - e2e/periods.spec.ts (login helper + waitForLoadState pattern to mirror verbatim)
    - lib/excel/template.ts (Plan 02-01 — used by the one-off fixture-building script below)
    - lib/db/plan-rows.ts (Plan 02-02 — exposes the typed surface we use to seed plan rows + executions for the blocked test)
    - lib/db/__smoke__/plan-upload.ts (Plan 02-02 — shows how to seed an execution against a plan row via raw SQL)
    - app/(app)/plans/upload/upload-form.tsx (Task 2 — the data-slot selectors the e2e queries)
  </read_first>
  <action>
    First, generate the fixture `e2e/fixtures/plan-counter-wall.xlsx`. Do this with a tiny one-off tsx script (commit it under `e2e/fixtures/build-fixtures.ts` OR inline it in the spec's `test.beforeAll` if Playwright tolerates fs writes — pick whichever keeps the fixture deterministic). The fixture has the counter-wall planColumns headers (from `buildPlanTemplate("counter-wall")` round-tripped through readWorkbook to verify, or written via `XLSX.utils.aoa_to_sheet` directly) PLUS two data rows with sfids "SF-A" and "SF-B" filled with valid Region/State/District/Taluka/Distributor/Dealer-or-Area values and a plausible Plan Sq Ft. The script writes the file once; commit the resulting `.xlsx` (it's a binary < 10 KB; checking it in is the standard pattern — see PATTERNS.md L78 verbatim).

    Add a NEW package.json script `"fixtures:build": "tsx e2e/fixtures/build-fixtures.ts"` so the fixture can be regenerated on demand (the ONLY package.json change in this plan).

    Then write `e2e/plans.spec.ts`. Copy the `login(page)` helper + `DEV_PASSWORD` from periods.spec.ts L1–16 verbatim. Two tests:

    **Test 1 — happy path (upload → preview → commit → list):**
    (1) `await login(page)`; go to `/periods` and create a fresh period (label includes Date.now() to keep tests independent); make it active.
    (2) Go to `/plans/upload`; verify the activity selector defaults to a real activity and the period selector defaults to the active period.
    (3) Select activity "Counter Wall Painting"; `page.setInputFiles("input[type=file]", "e2e/fixtures/plan-counter-wall.xlsx")`.
    (4) Wait for the preview to render: `await expect(page.locator('[data-slot="preview-row"]')).toHaveCount(2)`; assert both rows show classification "valid".
    (5) Click the "Commit upload" button; `await page.waitForLoadState("networkidle")`.
    (6) Assert a success block is visible mentioning "Inserted 2".
    (7) Go to `/plans`; assert the (counter-wall, our period) cell shows "2 rows".

    **Test 2 — blocked-by-actuals (closes COMP-02 path):**
    (1) `await login(page)`; reuse the same period setup (or fresh).
    (2) Upload + commit `plan-counter-wall.xlsx` exactly as Test 1 (sets up the 2 plan_rows).
    (3) Seed an execution against SF-A's plan_row by hitting a small TEST-ONLY helper: since this plan doesn't ship an actuals UI yet, expose an executions-seed in `lib/db/plan-rows.ts` as `_seedExecutionForTest(planRowId)` (mirroring `_resetPlanRowsForTest`); the e2e calls it via a tiny `/api/test/seed-execution` Route Handler that is GATED behind `process.env.NODE_ENV !== "production"` (Playwright runs against dev). [Alternative: trigger the seed by running the smoke script `npm run plan-upload:smoke` in a separate process before the e2e — but that resets state, so prefer the gated test-only Route Handler.] CRITICAL: the Route Handler must require the same session cookie + reject when NODE_ENV is "production" (defense-in-depth — test seam, not a back door). Document this in the Route Handler with a comment referencing this plan.
    (4) Build a one-row fixture `plan-counter-wall-only-b.xlsx` (or generate it inline in the test from buildPlanTemplate("counter-wall") + a single SF-B row via SheetJS write — Client side of the browser, NOT the server) and upload it.
    (5) Click "Commit upload"; `await page.waitForLoadState("networkidle")`.
    (6) Assert `await expect(page.locator('[data-slot="blocked-dealers"]')).toBeVisible()` AND its text contains "SF-A" AND mentions the execution-blocker message.
    (7) Go back to `/plans`; assert the (counter-wall, period) cell STILL shows "2 rows" (rollback held — no destructive write).

    Each test logs in fresh (cookies cleared at the top of login). Use deterministic labels for any data created in the test so re-runs work.

    OPTIONAL but RECOMMENDED: if generating a one-row .xlsx in the browser inside Test 2 proves fragile, commit a second fixture `e2e/fixtures/plan-counter-wall-only-b.xlsx` and use `setInputFiles` against it. This keeps the test deterministic.
  </action>
  <verify>
    <automated>npm run e2e -- --grep "plans"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run e2e -- --grep "plans"` runs both Plan tests and they PASS.
    - `e2e/fixtures/plan-counter-wall.xlsx` exists and is < 50 KB (a tiny pre-built fixture; eyeball-check `dir e2e/fixtures` or `Get-ChildItem e2e/fixtures`).
    - `Select-String -Path e2e/plans.spec.ts -Pattern "setInputFiles"` matches AND `-Pattern "blocked-dealers"` matches AND `-Pattern "SF-A"` matches.
    - `Select-String -Path e2e/plans.spec.ts -Pattern "login\\(page\\)"` matches (login helper used) AND each test starts with `await login(page)`.
    - If a test-only Route Handler was added under `app/api/test/`: `Select-String -Path "app/api/test/**/*.ts" -Pattern "NODE_ENV.*production"` matches — the seam is gated.
    - `git diff package.json` shows ONLY the `fixtures:build` script (or no change if you inline the fixture build) — NO dependency change.
    - Re-running the spec twice in a row passes both times (idempotent setup).
  </acceptance_criteria>
  <done>Happy path + blocked-by-actuals path both pass; COMP-02's user-facing surface is proven end-to-end against the real upload pipeline; the rollback invariant from Plan 02-02 is visible through the UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser file picker → upload-form.tsx | Untrusted `.xlsx` ArrayBuffer enters the client-side parse pipeline (10 MB cap, in-process). |
| Upload form → commitPlanUploadForm | Already-parsed JSON rows cross to the Server Action (which re-validates via PLAN_ROW_SCHEMAS — defense-in-depth from Plan 02-02). |
| Browser template-button click → SheetJS write | Pure client operation; no server hop, no upload, no shared state. |
| Test-only Route Handler (if added) → DB seam | Must be gated by NODE_ENV !== production AND require session — test seam, not a back door. |
| App layer → blocked-dealer rendering | The blockedDealers payload from the Server Action is rendered in the COMP-02 transient UI; no persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering (file-format) | A crafted `.xlsx` triggering CVE-2023-30533 in the browser parser | accept | Pinned to SheetJS CE 0.20.3 which contains the fix (vulnerable range is `<0.19.3`); D2-06 keeps parsing client-side so server-side blast radius is zero. The 10 MB cap (RESEARCH §6) bounds memory. |
| T-02-03-02 | Tampering (file-format) | A future contributor adds a Route Handler that parses `.xlsx` server-side | mitigate | Acceptance gate: `Select-String -Path "lib/db/**/*.ts","lib/actions/**/*.ts" -Pattern "from \"xlsx\""` returns ZERO matches; only `lib/excel/*` and `app/(app)/plans/upload/*.tsx` (Client) may import SheetJS. The security header comment from Plan 02-01 lives in lib/excel/parse.ts. |
| T-02-03-03 | Spoofing | Test-only Route Handler reachable in production | mitigate | If added: explicit `if (process.env.NODE_ENV === "production") return new Response("Not Found", { status: 404 })` AND require the standard session cookie. Acceptance gate verifies the NODE_ENV check is present. |
| T-02-03-04 | Tampering | Browser bypasses the preview's classification and posts dirty data to commitPlanUpload | mitigate | Server Action (Plan 02-02) re-runs `PLAN_ROW_SCHEMAS[activity]` on every row; `.strict()` rejects unknown keys; this UI is a UX aid, not a security boundary (RESEARCH "Common Pitfalls" C). |
| T-02-03-05 | Information Disclosure | blockedDealers exposes dealer SFIDs in the UI | accept | SFIDs and execution counts are operational metadata the team already sees in the upload Excel; intended surface (COMP-02). No PII, no secrets, no per-user data. |
| T-02-03-06 | Denial of Service | Pasting a 100 MB `.xlsx` into the file input | accept | 10 MB hard cap before `arrayBuffer()` (RESEARCH §6); larger files surface "File larger than 10 MB" without allocation. v1 doesn't need Web Worker offload (CONTEXT line 45). |
| T-02-03-SC | Tampering | npm installs | mitigate | This plan installs NOTHING. Acceptance gates assert `git diff package.json` has no dep change (only the optional `fixtures:build` script). |

No HIGH-severity threats. Server-side .xlsx exclusion (D2-06) is the load-bearing mitigation and is mechanically enforced by the acceptance gate.
</threat_model>

<verification>
- `npm run build` compiles with /plans and /plans/upload routes present.
- `npm test` green (no regressions in periods/items/excel/plans Vitest suites).
- `npm run e2e -- --grep "plans"` passes both tests (happy path + blocked-by-actuals).
- No `import * as XLSX from "xlsx"` or `from "xlsx"` anywhere outside `lib/excel/*` and `app/(app)/plans/upload/*.tsx` Client Components.
- `app/(app)/layout.tsx` carries the /plans nav link AND the existing /periods + /items + logout strip is unchanged.
- If a test-only seed Route Handler was added: it is gated by `NODE_ENV !== "production"` AND requires the session cookie.
- `git diff package.json` shows no dependency change (only the optional `fixtures:build` script).
</verification>

<success_criteria>
- PLAN-01 (template download): user can download `marketing-plan-template-{activity}.xlsx` for any of the six activities from /plans/upload; the e2e fixture is generated from the same pipeline so the contract is real.
- PLAN-02 (header validate UI): on upload, header mismatches surface as an inline parse error BEFORE any DB call (D2-06 + D2-03).
- PLAN-03 (preview UI): preview groups rows by classification (fieldError / duplicate / update / valid) with per-class counts and stable e2e selectors.
- COMP-02 (rejection surface, transient): the COMP-02 user-facing surface ships — the blocked-by-actuals path lists rejected dealers in the upload UI with their execution counts; persistence remains deferred per D2-02.
- Phase 2 success criterion 3 ("on commit, plan rows are saved in one transaction and become the allowed-SFID master list"): wired through Plan 02-02's `commitPlanUpload`, surfaced through this plan's UI; e2e proves the round-trip.
- Phase 2 success criterion 4 ("re-uploading preserves existing executions"): the blocked-by-actuals e2e is the user-facing proof; Plan 02-02's [BLOCKING] smoke is the structural proof.
</success_criteria>

<output>
Create `.planning/phases/02-plan-upload-and-periods/02-03-SUMMARY.md` when done.
</output>
