---
phase: 03-actuals-grid
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - app/(app)/layout.tsx
  - app/(app)/actuals/page.tsx
  - app/(app)/actuals/ag-grid-setup.ts
  - app/(app)/actuals/grid-loader.tsx
  - app/(app)/actuals/actuals-grid.tsx
  - app/(app)/actuals/filter-bar.tsx
  - app/(app)/actuals/save-bar.tsx
  - app/api/test/seed-execution/route.ts
  - e2e/actuals.spec.ts
autonomous: true
requirements: [GRID-01, GRID-02, GRID-03, GRID-05, GRID-07]

must_haves:
  truths:
    - "A user picks an activity for the active period and sees its plan rows in an editable AG Grid (plan columns read-only, actual columns editable inline)"
    - "All planned dealers are visible on open; a zero-execution dealer shows one empty ready-to-fill row; + add unit clones the dealer's plan context"
    - "The custom filter bar (cascading Region/State/District/Distributor/Status + SFID search) narrows the visible rows in-browser"
    - "Editing cells marks them dirty; the Save bar shows the unsaved count; Save persists and a reload shows the persisted values"
    - "A stale-version Save surfaces a 'reload this row' conflict on that unit without clobbering it, while other edits save"
  artifacts:
    - path: "app/(app)/actuals/actuals-grid.tsx"
      provides: "use client AG Grid: registry colDefs, derived valueGetters, dirty tracking, add-unit, Save orchestration"
      min_lines: 90
    - path: "app/(app)/actuals/page.tsx"
      provides: "Server Component: active period + activity selector → loads plan rows + executions → flat row model → grid"
      min_lines: 40
    - path: "e2e/actuals.spec.ts"
      provides: "e2e: edit→Save→reload persists; stale-version conflict surfaces + holds"
      min_lines: 60
  key_links:
    - from: "app/(app)/actuals/actuals-grid.tsx"
      to: "lib/actions/executions.saveExecutionsBatch"
      via: "Save bar posts the dirty UnitRow patch array; clears dirty on savedIds, flags conflicts"
      pattern: "saveExecutionsBatch"
    - from: "app/(app)/actuals/actuals-grid.tsx"
      to: "lib/actuals/{colDefs,calc,rows,filter}"
      via: "buildColumnDefs + computeDerived + buildRowModel + cascading filter predicates"
      pattern: "buildColumnDefs"
    - from: "app/(app)/layout.tsx"
      to: "/actuals"
      via: "nav Link added next to Periods/Plans/Items"
      pattern: "/actuals"
---

<objective>
Ship the working actuals grid end-to-end: the thinnest complete vertical slice where a user opens an activity for the active period, edits actuals inline in AG Grid, sees dirty state, clicks Save, and the values persist (surviving reload) — with the D3-11 conflict path surfaced. This wires together everything the prior waves built: the AG Grid recipe proven in the spike (03-01), the pure colDefs/calc/rows/filter core (03-02), and the version-safe save action (03-03). It covers the measurement + status activities inline; POP's multi-item modal and the Dealer-Certificate polish land in 03-05.

Purpose: This is the daily-use core of the whole product (ROADMAP Phase 3 goal). After this plan a user can actually record on-ground executions against the plan.
Output: `/actuals` route (page + AG Grid client + filter bar + save bar + module setup), the "Actuals" nav link, an extended seed route for e2e, and the edit→save→reload + conflict e2e.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-actuals-grid/03-CONTEXT.md
@.planning/phases/03-actuals-grid/03-RESEARCH.md
@.planning/phases/03-actuals-grid/03-01-SUMMARY.md
@.planning/phases/03-actuals-grid/03-02-SUMMARY.md
@.planning/phases/03-actuals-grid/03-03-SUMMARY.md

<interfaces>
<!-- Concrete identifiers. Read the three prior SUMMARYs for exact signatures; the essentials: -->

The AG Grid integration recipe (FROM 03-01-SUMMARY — reuse verbatim, do not rediscover):
  - app/(app)/actuals/ag-grid-setup.ts: ModuleRegistry.registerModules([AllCommunityModule]) (or the tree-shaken set the spike confirmed), imported once.
  - app/(app)/actuals/grid-loader.tsx: next/dynamic(() => import("./actuals-grid"), { ssr:false, loading: ... }) at the placement the spike confirmed works in Next 16 (A3 finding).
  - actuals-grid.tsx is "use client"; import ag-grid-setup; default theme (no CSS) OR the legacy escape hatch per the A4 finding; getRowId={(p)=>p.data.rowKey}; binding shape (dotted vs flattened) per the A1 finding.

From lib/actuals/ (03-02-SUMMARY for exact signatures):
  - buildColumnDefs(cfg, activityKey): ColDef[]  (plan read-only, actual editable, derived overridable valueGetter)
  - computeDerived(activityKey, key, fields), isOverridden/setOverride/clearOverride
  - buildRowModel(planRows, executions): UnitRow[]  (placeholders for zero-exec dealers); cloneUnitForAdd(row)
  - type UnitRow = { rowKey; planRowId; executionId|null; version; plan; fields; isPlaceholder; dirty }
  - filter: optionsFor(rows, col, upstream), matchesFacets(row, selected), matchesSfid(row, search), FacetKey, cascade order

From lib/actions/executions.ts (03-03-SUMMARY for exact contract):
  - saveExecutionsBatch(_prev, { activity, periodId, units: UnitPatch[] }): Promise<SaveBatchState>
  - SaveBatchState = { ok:true; savedIds:{rowKey;id;version}[]; conflicts:number[] } | { ok:false; error }
  - UnitPatch shape (rowKey, planRowId, executionId, version, fields, isPlaceholder, popLines?)

From lib/db/plan-rows.ts: listByPeriodActivity(periodId, activity): Promise<PlanRowRecord[]>
From lib/db/executions.ts: listExecutionsByPeriodActivity(periodId, activity): Promise<ExecutionRecord[]>; _findExecutionForTest(planRowId) (for e2e conflict setup)
From lib/periods/active.ts: getActivePeriod(): Promise<PeriodRow | null>
From lib/activities/registry.ts: ACTIVITY_KEYS, getActivity(key), ACTIVITIES (label per key)

Page pattern to MIRROR (app/(app)/plans/page.tsx): `export const dynamic = "force-dynamic"`; default async Server Component; getActivePeriod() → if none, render the "create/mark a period active in /periods" empty state; data-slot attributes for e2e selectors.

Nav pattern (app/(app)/layout.tsx lines 41-52): the Plans/Items <Link> blocks with className "rounded-md border border-neutral-200 px-3 py-1.5 hover:bg-neutral-50". Add an identical "Actuals" link to /actuals.

Client form pattern (app/(app)/items/item-form.tsx): "use client", useActionState for the action, formRef reset on success. The Save bar uses useActionState(saveExecutionsBatch, ...) or useTransition + a direct call — match the established useActionState ergonomics where it fits; the grid edit state is plain React state (a Map<rowKey, UnitRow>).

e2e conventions (e2e/plans.spec.ts — read it): DEV_PASSWORD = "jsw-marketing-2026"; login() clears cookies, fills input[name=password], submits, waits for "/"; createActivePeriod() creates+activates a month period via /periods UI; data-slot selectors throughout; single worker; .pglite wiped before dev. Seed via the gated POST /api/test/seed-execution (NODE_ENV!=production + session + POST).

The seed route (app/api/test/seed-execution/route.ts) currently only seeds a bare execution and returns { planRowId }. EXTEND it (keep all three gates) so the e2e can seed a known execution and read back its id+version for the conflict test (e.g. return the execution id+version, or add an action param). Do NOT weaken the gates.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Actuals page (Server Component) + AG Grid client wired to the registry + nav link</name>
  <files>app/(app)/layout.tsx, app/(app)/actuals/page.tsx, app/(app)/actuals/ag-grid-setup.ts, app/(app)/actuals/grid-loader.tsx, app/(app)/actuals/actuals-grid.tsx</files>
  <read_first>
    - .planning/phases/03-actuals-grid/03-01-SUMMARY.md (the working AG Grid recipe — module set, ssr:false placement A3, theme choice A4, binding A1)
    - .planning/phases/03-actuals-grid/03-02-SUMMARY.md (buildColumnDefs/buildRowModel/computeDerived signatures)
    - app/(app)/plans/page.tsx (Server Component shape, active-period empty state, force-dynamic, data-slot selectors)
    - app/(app)/layout.tsx (nav Link blocks to mirror for the Actuals entry)
    - app/(app)/items/item-form.tsx ("use client" + useActionState ergonomics)
    - .planning/phases/03-actuals-grid/03-RESEARCH.md (§"AG Grid Setup" lines 452-485; §Pattern 1 lines 237-275; §Pattern 3 valueGetter/valueSetter/cellClassRules lines 341-361; §Pitfall 7 SSR lines 550-553; §Anti-Patterns lines 487-494)
  </read_first>
  <action>
    Add the "Actuals" nav entry to app/(app)/layout.tsx — a <Link href="/actuals"> styled identically to the Plans/Items links, placed next to them (D3 nav discretion).

    Create app/(app)/actuals/page.tsx as a Server Component (mirror plans/page.tsx: `export const dynamic = "force-dynamic"`):
      - getActivePeriod(); if null, render the empty state pointing to /periods (same copy/shape as plans/page.tsx) with a data-slot for the e2e.
      - Read the selected activity from searchParams (`?activity=<key>`), defaulting to the first ACTIVITY_KEYS entry. Render an activity selector (links or a form, mirroring how /plans cells link with ?activity=) carrying data-slot="activity-select".
      - Load `listByPeriodActivity(activePeriod.id, activity)` and `listExecutionsByPeriodActivity(activePeriod.id, activity)`, build the initial row model via `buildRowModel(planRows, executions)` (server-side assembly so the client gets ready rows), and pass rows + activityKey + periodId to the dynamically-imported grid. For POP/item-list activities, also pass the ACTIVE item master (listItems().filter(active)) — even though the modal ships in 03-05, passing it now keeps the page contract stable; the grid can ignore it until 03-05.
      - Use data-slot attributes for e2e: "actuals-page", "actuals-grid" (on the grid container), "activity-select".

    Create app/(app)/actuals/ag-grid-setup.ts and grid-loader.tsx exactly per the 03-01 recipe (module registration; next/dynamic ssr:false at the confirmed placement).

    Create app/(app)/actuals/actuals-grid.tsx as "use client":
      - import "./ag-grid-setup"; render <AgGridReact> with columnDefs = buildColumnDefs(getActivity(activityKey)!, activityKey), rowData from props, getRowId={(p)=>p.data.rowKey}.
      - Hold edit state in React: a Map<rowKey, UnitRow> seeded from props; onCellValueChanged updates the row's fields, marks dirty:true, and (for non-overridden derived cells) lets the valueGetter recompute; a manual edit of a derived cell calls setOverride (sticky, D3-05) and a "reset to formula" affordance calls clearOverride + refreshes the cell.
      - Add a "+ add unit" affordance per dealer (action cell button or a per-dealer add control) that calls cloneUnitForAdd and appends the new row (D3-03).
      - Apply cellClassRules for dirty + overridden cells (visual treatment, D3-05/D3-12). Use the binding shape (dotted/flattened) and theme per the spike findings. Do NOT import legacy AG Grid CSS if the spike chose the default theme (Anti-Pattern: double CSS).
      - Render the <FilterBar> (Task 2) above the grid and the <SaveBar> (Task 2) wired to the dirty Map; this task may stub their props and Task 2 fills them in, or build all three together — they share actuals-grid.tsx state so coordinate within this plan.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && npm run build</automated>
  </verify>
  <done>`/actuals` is in the nav and builds; the page loads plan rows + executions for the active period + selected activity, assembles the flat row model server-side, and renders an editable AG Grid with read-only plan columns, editable actual columns, derived valueGetters, dirty highlighting, and a + add unit affordance.</done>
</task>

<task type="auto">
  <name>Task 2: Cascading filter bar + dirty Save bar wired to saveExecutionsBatch</name>
  <files>app/(app)/actuals/filter-bar.tsx, app/(app)/actuals/save-bar.tsx, app/(app)/actuals/actuals-grid.tsx</files>
  <read_first>
    - app/(app)/actuals/actuals-grid.tsx (the grid + edit-state Map created in Task 1 — filter bar and save bar plug into it)
    - .planning/phases/03-actuals-grid/03-02-SUMMARY.md (optionsFor/matchesFacets/matchesSfid/FacetKey + cascade order)
    - .planning/phases/03-actuals-grid/03-03-SUMMARY.md (saveExecutionsBatch contract: UnitPatch, SaveBatchState, savedIds/conflicts semantics)
    - .planning/phases/03-actuals-grid/03-RESEARCH.md (§Pattern 4 external filter lines 364-386; §Pattern 5 Save + conflict surfacing lines 388-430; §Pitfall 4 override loop lines 532-536)
    - app/(app)/items/item-form.tsx (useActionState + pending + success-reset ergonomics)
  </read_first>
  <action>
    Create app/(app)/actuals/filter-bar.tsx ("use client") per D3-06/07/08:
      - Multi-select dropdowns for Region / State / District / Distributor / Status whose option lists come from optionsFor(loadedRows, col, upstreamSelections) so they CASCADE (Region narrows State narrows District). Distributor + Status are independent facets.
      - An SFID search input bound to matchesSfid (dedicated predicate — NOT AG Grid quickFilter over all columns, per the A6 finding).
      - On any change, drive AG Grid's external filter: wire gridApi `isExternalFilterPresent` (true when any facet/search active) + `doesExternalFilterPass(node)` = matchesFacets(node.data, selected) && matchesSfid(node.data, search); call `gridApi.onFilterChanged()` to re-run. Use data-slot attributes for e2e: "filter-bar", per-facet "filter-region" etc., "sfid-search".

    Create app/(app)/actuals/save-bar.tsx ("use client") per D3-10/11/12:
      - A persistent bar showing "N unsaved changes" (N = count of dirty rows in the Map) with a Save button (disabled when N=0 or pending). data-slot="save-bar", the count in data-slot="unsaved-count", the button data-slot="save-button".
      - On Save: build the UnitPatch[] from dirty rows (rowKey, planRowId, executionId, version, fields, isPlaceholder; popLines only for POP), filtering out untouched placeholders client-side too (server also filters, but avoid sending noise). Call saveExecutionsBatch via useActionState (or useTransition). On the result: for each savedId, clear that row's dirty flag and update its executionId+version from the return (so a subsequent edit carries the new version); for each id in conflicts, flag that row "reload this row" (data-slot="row-conflict") and do NOT clear its dirty/value (D3-11 block-not-overwrite). Show a "saved" confirmation (data-slot="save-confirmation") after a clean flush.
      - A conflict-row "reload" affordance re-fetches that unit's current server value (e.g. via a router.refresh() of the page or a targeted reload) so the user can retry against the fresh version.

    Wire both components into actuals-grid.tsx: the filter bar reads the loaded rows + drives the grid api; the save bar reads the dirty Map + applies the save result back into grid state. Guard against the Pitfall-4 override loop: the derived valueSetter only stores the value + flips the override flag; it never re-derives.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && npm run build</automated>
  </verify>
  <done>The filter bar's dropdowns cascade and the SFID search scopes to plan.sfid; the Save bar shows the live unsaved count, posts dirty units to saveExecutionsBatch, clears dirty + updates version on savedIds, flags conflict rows "reload this row" without clobbering, and shows a saved confirmation; the build passes.</done>
</task>

<task type="auto">
  <name>Task 3: e2e — edit→Save→reload persists; stale-version conflict surfaces and holds</name>
  <files>app/api/test/seed-execution/route.ts, e2e/actuals.spec.ts</files>
  <read_first>
    - e2e/plans.spec.ts (login(), createActivePeriod(), data-slot selectors, the seed-route call pattern with cookie header, networkidle waits — clone this structure)
    - app/api/test/seed-execution/route.ts (the three gates to preserve; extend to seed a known execution + return id/version for the conflict test)
    - app/(app)/actuals/actuals-grid.tsx + save-bar.tsx + filter-bar.tsx (the data-slot selectors to target)
    - .planning/phases/03-actuals-grid/03-RESEARCH.md (§Testing Strategy lines 653-668 — the e2e rows; §Wave 0 Gaps lines 674-683)
    - e2e/fixtures/build-fixtures.ts + the existing plan-counter-wall.xlsx fixture (reuse counter-wall as the e2e activity so a plan exists to record actuals against)
  </read_first>
  <action>
    Extend app/api/test/seed-execution/route.ts (keep NODE_ENV!=production + session + POST-only): support seeding a known execution against a plan_row and returning its id + version, so the e2e can later post a STALE version through the UI/path and assert the conflict. Add a `_findExecutionForTest`-backed lookup (from lib/db/executions.ts) if needed. Do NOT weaken any gate; do NOT reference this route from app code.

    Create e2e/actuals.spec.ts cloning plans.spec.ts conventions (login, createActivePeriod, DEV_PASSWORD "jsw-marketing-2026", single worker). Tests:
      1. edit→Save→reload persists: login → create active period → upload the counter-wall fixture via /plans/upload + commit (reuse the plans.spec helper flow) so plan rows exist → go to /actuals?activity=counter-wall → edit an actual cell (e.g. actualSqft) on a placeholder row → assert the Save bar shows an unsaved count → click Save → assert the saved confirmation → reload /actuals → assert the edited value is still present (persisted as a real execution).
      2. derived auto-calc visible: after entering length+breadth (use an L×B activity like in-shop, or actualSqft+perUnitCost for counter-wall) assert the derived total cell shows the computed value before Save.
      3. stale-version conflict holds (D3-11): seed an execution against a known SFID via the gated route and capture its version; in the grid, drive a Save for that unit carrying the stale version (simulate by editing the row then have the route/another path bump the server version first, per the seed-route extension) → assert a "reload this row" conflict marker appears on that unit, the unit's value is NOT overwritten, and a sibling edited unit DID save. (If simulating the race purely through the UI is impractical, assert the conflict path via the seed-route-bumped version + a Save, matching the RESEARCH e2e intent.)
    Use data-slot selectors only (no text-coupling beyond what plans.spec does).
  </action>
  <verify>
    <automated>npm test && npm run e2e -- e2e/actuals.spec.ts</automated>
  </verify>
  <done>The full vitest sweep stays green AND e2e/actuals.spec.ts passes: edit→Save→reload shows the persisted value, the derived total renders live before save, and the stale-version Save surfaces "reload this row" without clobbering while a sibling unit saves; the seed route keeps all three gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /actuals page + Server Action | User-supplied measurements/costs/status cross into the app behind the shared-password gate |
| test-only seed route → DB | The gated e2e seed route writes executions (dev/test only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | Lost-update on shared login (grid Save) | mitigate | Save bar posts per-unit patches carrying version; conflicts from saveExecutionsBatch are surfaced as "reload this row" and NOT cleared/overwritten (D3-11). Proven by the e2e conflict test |
| T-03-06 | Spoofing / Elevation | Page render + grid access | mitigate | The (app) layout re-verifies the session on every render (CVE-2025-29927 defense-in-depth); the save action also calls requireSession (03-03) |
| T-03-05 | Tampering / Elevation | Off-plan write from the grid | mitigate | The grid only ever sends planRowId (from loaded plan rows); no sfid is sent or accepted; FK enforces it structurally (COMP-01) |
| T-03-10 | Elevation of Privilege | Test-only seed route as a back door | mitigate | Three gates preserved: NODE_ENV!=production (404 in prod), session-required (401), POST-only (405). Extension does not relax them; never referenced from app code |
| T-03-11 | Information Disclosure | Client-side full dataset load (D3-08) | accept | ≤~1k rows of already-authorized period data loaded into the authenticated browser; no cross-period or cross-tenant data; revisit only at ~5-10k rows (deferred) |
</threat_model>

<verification>
- `npm run build` passes (the App Router compiles the dynamic ssr:false grid, page, filter bar, save bar).
- `npm test` full sweep green (no regression from the new route/page).
- `npm run e2e -- e2e/actuals.spec.ts` green: edit→Save→reload persists; derived total renders live; stale-version conflict surfaces + holds + sibling saves.
- Code assertions: an "Actuals" link to /actuals exists in layout.tsx; the grid posts only planRowId (no sfid); the seed route retains all three gates; no legacy AG Grid CSS import if the spike chose the default theme.
</verification>

<success_criteria>
- A user views a period's plan rows for an activity in an editable grid; plan columns read-only, actual columns editable inline (GRID-01 / GRID-02).
- All planned dealers visible; zero-execution dealers show a placeholder row; + add unit clones plan context (GRID-05 / D3-02 / D3-03).
- Cascading filter bar + SFID search narrow rows in-browser (GRID-03 / D3-06/07/08).
- Edits are dirty-tracked; Save persists via the version-safe action; reload shows persisted values; conflicts surface without clobbering (GRID-07 / D3-10/11/12).
- "Actuals" nav entry added; e2e proves the slice end to end.
</success_criteria>

<output>
Create `.planning/phases/03-actuals-grid/03-04-SUMMARY.md` when done. Record: the final AG Grid wiring that worked (binding shape, theme, ssr:false placement as actually used), the data-slot selector inventory for /actuals (so 03-05 reuses them), how the Save bar maps savedIds/conflicts back into grid state, and the page's props contract (rows + activityKey + periodId + items) so 03-05 plugs the POP modal in without changing the page signature.
</output>
