---
phase: 03-actuals-grid
plan: 05
type: execute
wave: 4
depends_on: ["03-02", "03-03", "03-04"]
files_modified:
  - app/(app)/actuals/pop-modal.tsx
  - app/(app)/actuals/actuals-grid.tsx
  - e2e/actuals.spec.ts
autonomous: true
requirements: [GRID-06, GRID-08]

must_haves:
  truths:
    - "A POP/Dealer-Kit dealer row opens a popup where the user adds multiple line items (item from the ACTIVE item master, qty, rate) with Line Total = qty × rate auto-filling"
    - "The popup subtotal rolls up to the dealer's kit row total and persists as one execution + N execution_items (itemName snapshot) on Save"
    - "Dealer Certificate entry captures Status + Date + Cost inline on the execution row (no popup, no line items)"
  artifacts:
    - path: "app/(app)/actuals/pop-modal.tsx"
      provides: "Tailwind modal: ACTIVE item picker, add/remove lines, qty×rate line totals, subtotal rollup"
      min_lines: 70
  key_links:
    - from: "app/(app)/actuals/pop-modal.tsx"
      to: "lib/actions/executions.saveExecutionsBatch (popLines)"
      via: "kit save sends popLines; persists execution + execution_items atomically"
      pattern: "popLines"
    - from: "app/(app)/actuals/pop-modal.tsx"
      to: "item master (ACTIVE only)"
      via: "picker lists active items; itemName snapshotted at entry (D-08)"
      pattern: "itemName"
---

<objective>
Complete the phase with the two activity shapes that don't fit plain inline cells: the POP/Dealer-Kit multi-item kit (a popup of line items rolled up to the dealer row) and the Dealer Certificate (Status + Date + Cost inline). The grid, save action, and item master are already in place from prior waves; this plan adds the POP modal, wires the item-list activity's "kit" row to open it, confirms the status-activity inline path, and proves both with e2e. After this plan all of GRID-01..08 are delivered.

Purpose: POP/Dealer-Kit and Dealer Certificate are two of the six activities; without them the grid is incomplete for the team's real work. The POP popup is the last bespoke UI; everything else is the registry-driven inline grid.
Output: `app/(app)/actuals/pop-modal.tsx`, the kit-row wiring in the grid, and the POP e2e (add 2 lines → rollup → persist).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-actuals-grid/03-CONTEXT.md
@.planning/phases/03-actuals-grid/03-RESEARCH.md
@.planning/phases/03-actuals-grid/03-02-SUMMARY.md
@.planning/phases/03-actuals-grid/03-03-SUMMARY.md
@.planning/phases/03-actuals-grid/03-04-SUMMARY.md

<interfaces>
<!-- Concrete identifiers. Read 03-03/03-04 SUMMARYs for exact contracts; essentials: -->

POP/Dealer-Kit activity config (lib/activities/pop-dealer-kit.ts) — type "item-list":
  actualColumns (PER-LINE shape): itemName(text), qty(number), rate(currency "Per-unit cost"), lineTotal(currency, computeFrom ["qty","rate"])
  → One dealer = ONE execution ("kit") carrying N execution_items (D3-13). The flat-grid unit row for a POP dealer represents that kit; an "edit" affordance opens this modal. POP executions leave status null.

Dealer Certificate config (lib/activities/dealer-certificate.ts) — type "status":
  actualColumns: status(status, enumValues Pending/In Progress/Done — "Done"="Issued" per D3-09), issuanceDate(date), cost(currency)
  → Inline only, NO popup, NO line items (D3-15). This should ALREADY work via the registry-driven inline grid from 03-04; this plan VERIFIES it and adds a focused e2e, fixing only if the status-type path has a gap.

From lib/db/items.ts: listItems(): Promise<ItemRow[]> where ItemRow = { id; name; category|null; active }. Picker shows ACTIVE only (filter active===true). itemName is SNAPSHOT text at entry (D-08) — store the name string, never the item id/FK.

From lib/actuals/calc.ts (03-02): computeDerived("pop-dealer-kit","lineTotal",{qty,rate}) === qty×rate. Use it for live line totals in the modal.

From lib/actions/executions.ts (03-03-SUMMARY): saveExecutionsBatch accepts a UnitPatch with `popLines: { itemName, qty, rate, lineTotal }[]`; savePopKit persists one execution + N execution_items atomically (replace-all). The kit row total = sum of lineTotals.

From 03-04-SUMMARY: the page already passes the ACTIVE item master to the grid for item-list activities; the grid's edit-state Map<rowKey, UnitRow>; the Save bar builds UnitPatch[] from dirty rows (extend it to include popLines for POP kit rows); the data-slot selector inventory for /actuals; how savedIds/conflicts map back to grid state.

Modal pattern (NO shadcn — Tailwind only, per project convention; app/(app)/items/item-form.tsx for the form/useActionState/reset ergonomics and the Tailwind class vocabulary). The POP modal is a plain Tailwind overlay.

e2e (e2e/actuals.spec.ts from 03-04 — extend it): login/createActivePeriod helpers; DEV_PASSWORD "jsw-marketing-2026"; seed plan rows by uploading a POP fixture OR via the existing upload flow; data-slot selectors. To have ACTIVE items, add one or two via /items UI in the test (item-form) or seed them. Reuse the existing fixture-build approach if a POP fixture is needed (e2e/fixtures/build-fixtures.ts).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: POP/Dealer-Kit multi-item modal + kit-row wiring</name>
  <files>app/(app)/actuals/pop-modal.tsx, app/(app)/actuals/actuals-grid.tsx</files>
  <read_first>
    - .planning/phases/03-actuals-grid/03-04-SUMMARY.md (grid edit-state Map, Save-bar UnitPatch building, page→grid item-master prop, data-slot inventory)
    - .planning/phases/03-actuals-grid/03-03-SUMMARY.md (saveExecutionsBatch popLines contract + savePopKit semantics)
    - lib/activities/pop-dealer-kit.ts (the per-line FieldDef shape: itemName, qty, rate, lineTotal)
    - lib/db/items.ts (listItems / ItemRow / active flag — picker is ACTIVE-only)
    - lib/actuals/calc.ts (computeDerived lineTotal = qty×rate for live totals)
    - app/(app)/items/item-form.tsx (Tailwind + useActionState + reset ergonomics to mirror for the modal)
    - .planning/phases/03-actuals-grid/03-RESEARCH.md (§Pattern 6 POP modal + savePopKit lines 432-450; §Don't Hand-Roll item snapshot note)
  </read_first>
  <action>
    Create app/(app)/actuals/pop-modal.tsx ("use client") per D3-13/14:
      - A plain Tailwind modal overlay (no shadcn) opened for a given POP kit row, receiving: the dealer's plan context (read-only header), the current line items (from the row's popLines / fields), and the ACTIVE item master (passed from the grid).
      - A line-item editor: each line = an item picker (a <select> listing ACTIVE items by name — store the chosen NAME as a snapshot, never the id, per D-08), a Qty input, a Rate input ("Per-unit cost"), and a read-only Line Total = computeDerived("pop-dealer-kit","lineTotal",{qty,rate}) that auto-fills as qty/rate change. Add "+ add line" and per-line remove controls.
      - A subtotal = sum of line totals shown at the bottom; this rolls up to the kit row's total.
      - On confirm: write the lines back into the kit UnitRow (set its popLines + totalCost = subtotal), mark the row dirty, and close. Do NOT save directly — the existing Save bar flushes it (so POP saves go through the same batched, version-checked path). data-slot attributes: "pop-modal", "pop-line", "pop-add-line", "pop-line-total", "pop-subtotal", "pop-confirm".

    Wire into actuals-grid.tsx: for item-list activities (getActivity(activityKey).type === "item-list"), render the POP kit row with an "edit kit" affordance (a button cell showing the line count + rolled total, data-slot="pop-edit") that opens the modal for that row; pass the ACTIVE item master through. Extend the Save-bar UnitPatch construction (in save-bar.tsx via the shared grid state, or here) so a dirty POP kit row sends its `popLines` in the patch — the server's savePopKit then persists execution + execution_items atomically. A POP placeholder dealer with no lines is still a placeholder (not persisted until lines are added — D3-02 applies).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && npm run build</automated>
  </verify>
  <done>The POP kit row opens a Tailwind modal listing ACTIVE items; adding lines computes Line Total = qty×rate live; the subtotal rolls up to the kit row total; confirming marks the row dirty with popLines so the existing Save bar persists one execution + N execution_items with snapshotted item names; the build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Dealer Certificate inline path + POP and Dealer-Certificate e2e</name>
  <files>e2e/actuals.spec.ts, app/(app)/actuals/actuals-grid.tsx</files>
  <read_first>
    - lib/activities/dealer-certificate.ts (status + issuanceDate + cost; status enumValues from 03-02)
    - .planning/phases/03-actuals-grid/03-04-SUMMARY.md (the registry-driven inline grid — dealer-certificate should already render via it; data-slot inventory)
    - e2e/actuals.spec.ts (the 03-04 tests + helpers to extend)
    - e2e/plans.spec.ts (upload+commit flow to seed plan rows for an activity; the gated seed route usage)
    - e2e/fixtures/build-fixtures.ts (how fixtures are generated, if a POP / dealer-certificate fixture is needed)
    - .planning/phases/03-actuals-grid/03-RESEARCH.md (§Testing Strategy POP e2e line 667; §Wave 0 Gaps lines 674-683)
  </read_first>
  <action>
    Confirm the Dealer Certificate (status-type) activity records Status + Date + Cost inline through the registry-driven grid from 03-04 — no popup. If a gap exists (e.g. the status select or date editor isn't wired for status-type activities), fix it minimally in actuals-grid.tsx; otherwise no grid change is needed and this is verification-only.

    Extend e2e/actuals.spec.ts (clone the existing helpers) with two tests:
      1. POP kit (GRID-06): login → create active period → add 1-2 ACTIVE items via /items (item-form) → upload a POP plan (fixture) + commit so a POP dealer plan row exists → /actuals?activity=pop-dealer-kit → open the kit row's modal (data-slot="pop-edit") → add 2 line items (pick items, enter qty+rate) → assert each Line Total = qty×rate and the subtotal sums them → confirm → assert the kit row shows the rolled total and is dirty → Save → reload → assert the kit persisted (2 lines, rolled total). Optionally assert via the page that the execution + 2 execution_items exist (through the reloaded grid showing the line count).
      2. Dealer Certificate (GRID-08): login → create active period → upload a dealer-certificate plan + commit → /actuals?activity=dealer-certificate → set Status to "Done" (Issued), enter Date + Cost inline → Save → reload → assert the status/date/cost persisted on the row.
    Use data-slot selectors. If a POP or dealer-certificate xlsx fixture does not exist, add it via the build-fixtures script (mirror the counter-wall fixture).
  </action>
  <verify>
    <automated>npm test && npm run e2e -- e2e/actuals.spec.ts</automated>
  </verify>
  <done>The full vitest sweep stays green AND e2e/actuals.spec.ts passes including the POP-kit test (2 lines → live line totals → subtotal rollup → persist) and the Dealer-Certificate inline test (Status/Date/Cost persist on reload); Dealer Certificate records inline with no popup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Server Action (POP kit save) | User-supplied line items (item name, qty, rate) cross into the app via the batched save |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Tampering | POP line items (qty/rate/lineTotal) from the modal | mitigate | saveExecutionsBatch (03-03) Zod-validates popLines and recomputes lineTotal/subtotal server-side; the kit write is one transaction (savePopKit replace-all). The modal computes for display only |
| T-03-13 | Tampering | Item identity spoofing (retired/forged item) | mitigate | itemName is a TEXT SNAPSHOT at entry (D-08), never an FK to item_master; picker shows ACTIVE only but a previously-snapshotted name persists unaffected. Server stores the provided name string; no client item-id is trusted |
| T-03-05 | Tampering / Elevation | Off-plan POP write | mitigate | The kit execution attaches only via planRowId (NOT NULL FK); no sfid sent or accepted (COMP-01) |
| T-03-06 | Spoofing / Elevation | Modal + save access | mitigate | (app) layout re-verifies session on render; saveExecutionsBatch calls requireSession (03-03) |
</threat_model>

<verification>
- `npm run build` passes (POP modal + grid wiring compile).
- `npm test` full sweep green (no regression).
- `npm run e2e -- e2e/actuals.spec.ts` green incl. POP-kit (live line totals → subtotal rollup → persist as execution + execution_items) and Dealer-Certificate inline (Status/Date/Cost persist).
- Code assertions: the POP picker filters to ACTIVE items; itemName is stored as a name string (snapshot), not an id; POP kit saves flow through the shared version-checked Save bar (not a separate unguarded action).
</verification>

<success_criteria>
- POP/Dealer-Kit records multiple line items via a popup (item × qty × rate → line total), rolled up to the dealer's kit row, persisted as one execution + N execution_items with snapshotted names (GRID-06 / D3-13/14).
- Dealer Certificate captures issuance Status + Date + Cost inline, no popup (GRID-08 / D3-15).
- Both proven by e2e; all of GRID-01..08 now delivered across the phase.
</success_criteria>

<output>
Create `.planning/phases/03-actuals-grid/03-05-SUMMARY.md` when done. Record: the POP modal contract (props + popLines shape it writes back), confirmation that itemName is snapshotted (not an FK), whether the Dealer-Certificate inline path needed any fix or worked from the registry alone, and the final GRID-01..08 coverage status for the phase verifier.
</output>
