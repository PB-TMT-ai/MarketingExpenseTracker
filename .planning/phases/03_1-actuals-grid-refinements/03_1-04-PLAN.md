---
phase: 03_1-actuals-grid-refinements
plan: 04
type: execute
wave: 2
depends_on: ["03_1-02"]
files_modified:
  - app/(app)/actuals/save-bar.tsx
  - app/(app)/actuals/actuals-grid.tsx
  - lib/actuals/use-save-executions.ts
  - lib/actuals/calc.ts
  - lib/actuals/coerce.ts
  - lib/actuals/coerce.test.ts
  - e2e/actuals.spec.ts
autonomous: false
requirements: [GRID-12, GRID-13]
must_haves:
  truths:
    - "Save is reachable from a sticky bar at the TOP of the grid and the existing one at the BOTTOM"
    - "Both bars show the SAME dirty count and share ONE submit / one useActionState (no double-submit, no divergent counts)"
    - "Ctrl/Cmd+S triggers the same save as the bars"
    - "Pasting a TSV block from Excel at a focused cell fills across editable columns and down displayed rows; read-only plan.* cells are never written; overflow is silently dropped with a note"
  artifacts:
    - path: "lib/actuals/use-save-executions.ts"
      provides: "single useActionState save hook feeding both presentational bars"
      contains: "useActionState"
    - path: "app/(app)/actuals/save-bar.tsx"
      provides: "presentational SaveBar (props in, no internal useActionState) with data-slot save-bar-top/-bottom"
      contains: "data-slot"
    - path: "lib/actuals/coerce.ts"
      provides: "coerceForKind(raw, kind) for paste type coercion"
      contains: "coerceForKind"
    - path: "app/(app)/actuals/actuals-grid.tsx"
      provides: "two bars + Ctrl/Cmd+S keydown listener + DOM paste listener (applyTransaction bulk write)"
      contains: "applyTransaction"
  key_links:
    - from: "useSaveExecutions hook"
      to: "both SaveBar instances + Ctrl/Cmd+S"
      via: "one submit/pending/dirtyCount shared"
      pattern: "submit"
    - from: "DOM paste listener"
      to: "api.applyTransaction({ update: changedRows })"
      via: "getFocusedCell anchor + getDisplayedRowAtIndex walk + coerceForKind"
      pattern: "getFocusedCell"
---

<objective>
Deliver GRID-12 (save bar top + bottom, single source of truth) and GRID-13 (custom paste-block handler) — both of which modify the refactored `actuals-grid.tsx`, so this plan depends on Plan 02's GRID-09 state model (`rowsRef` + `dirtyKeys` + `applyTransaction`) landing first.

GRID-12 (D3.1-10): lift the save flow out of SaveBar into a `useSaveExecutions` hook (ONE `useActionState`, ONE submit, ONE onSaveResult), make SaveBar purely presentational, render TWO bars (sticky `top-0` above the grid + the existing sticky `bottom-0`) sharing the same `{dirtyCount, pending, lastResult, onSave}`, and add ONE Ctrl/Cmd+S keydown listener that calls the same submit. No double-submit, no divergent counts (LOCKED single-source-of-truth invariant).

GRID-13 (D3.1-11): a plain DOM `paste` listener on the grid container (AG Grid Community has NO clipboard/range/fill — all Enterprise, R7). It reads `clipboardData.getData('text/plain')`, parses Excel/Sheets TSV into a 2-D array, anchors on `getFocusedCell()`, maps onto EDITABLE (`fields.*`) columns left-to-right and DISPLAYED rows downward (respects sort/filter, R5), coerces per column kind, writes the whole block via ONE `applyTransaction({update: changedRows})`, marks rows dirty, and surfaces an overflow note. Derived cells set the override flag (reuse setOverride, LOCKED). It writes ONLY `fields.*` on EXISTING rows — never introduces an SFID (security invariant LOCKED).

Purpose: Save reachability on long grids (GRID-12) and bulk Excel entry (GRID-13) — the two operational asks. Both reuse Plan 02's batched edit path, so this plan runs in Wave 2 after GRID-09.

Output: `use-save-executions.ts` hook; presentational `save-bar.tsx` with top/bottom data-slots; two bars + Ctrl/Cmd+S + paste listener in `actuals-grid.tsx`; `coerce.ts` + `coerce.test.ts`; e2e for both bars and synthetic paste.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03_1-actuals-grid-refinements/03_1-CONTEXT.md
@.planning/phases/03_1-actuals-grid-refinements/03_1-RESEARCH.md
@.planning/phases/03_1-actuals-grid-refinements/03_1-02-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs — from the codebase + Plan 02's refactored grid. -->

From app/(app)/actuals/save-bar.tsx — CURRENT (owns its own useActionState; make it presentational):
```typescript
// Current props: { dirtyRows, activityKey, periodId, onSaveResult, items? }; internally builds units[] and calls saveExecutionsBatch via useActionState.
// data-slots present: save-bar, unsaved-count, save-button, save-confirmation. Markup: sticky bottom-0 flex ... rounded-lg border bg-white px-4 py-3 shadow-md.
```

From lib/actions/executions.ts — the action the hook wraps:
```typescript
export type SaveBatchState = { ok: true; savedIds: SavedUnit[]; conflicts: number[] } | { ok: false; error: string };
export async function saveExecutionsBatch(_prev: unknown, input: { activity: string; periodId: number; units: UnitPatch[] }): Promise<SaveBatchState>;
// UnitPatch (built today inside SaveBar from dirty rows): { rowKey, planRowId, executionId, version, fields, isPlaceholder, popLines? }
const INITIAL_STATE: SaveBatchState = { ok: true, savedIds: [], conflicts: [] };
```

From Plan 02's refactored actuals-grid.tsx — the state model THIS plan builds on:
```typescript
const rowsRef = useRef<Map<string, UnitRow>>(...);   // authoritative rows
const [dirtyKeys, setDirtyKeys] = useState<Set<string>>(...); // dirty rowKeys
const dirtyRows = useMemo(() => [...dirtyKeys].map(k => rowsRef.current.get(k)!).filter(Boolean), [dirtyKeys]);
const apiRef = useRef<GridApi<UnitRow>|null>(null);
// handleCellValueChanged already calls api.applyTransaction({update:[row]}) + setOverride for derived cells.
// getRowId={(p)=>p.data.rowKey} set; columnDefs built from buildColumnDefs (plan.* editable:false, fields.* editable:true, derived editable:true + valueGetter).
```

From lib/actuals/calc.ts — reuse for paste coercion + override:
```typescript
function num(v: unknown): number | null;  // strips ₹/commas → number|null. EXPORT it for coerce.ts (or duplicate the 3-line fn).
export function setOverride(f: Record<string,unknown>, key: string, on: boolean): void; // derived-cell paste → override (LOCKED)
```

AG Grid Community APIs for paste (VERIFIED Community; clipboard/range/fill are Enterprise — do NOT use processDataFromClipboard/onPasteStart, R7):
```typescript
api.getFocusedCell(): { rowIndex: number; column: Column } | null;  // anchor (rowIndex is a DISPLAYED index)
api.getAllDisplayedColumns(): Column[];          // ordered displayed columns
api.getDisplayedRowAtIndex(i): IRowNode | undefined;  // walk displayed rows (respects sort+filter, R5)
api.applyTransaction({ update: UnitRow[] });     // ONE call for the whole block
// Column: col.getColId(); col.getColDef() → { field?, editable?, valueGetter? }
```

FieldDef.kind ∈ text|number|currency|date|status|enum|lat|long (from lib/activities/types). counter-wall status enumValues = ["Pending","In Progress","Done"]. Date discipline: DD/MM/YY string (Phase 2 D2 — NEVER coerce to ISO). lat/long: NEVER numeric-coerce.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GRID-12 — useSaveExecutions hook + presentational SaveBar + two bars + Ctrl/Cmd+S</name>
  <read_first>
    - app/(app)/actuals/save-bar.tsx (FULL — current useActionState flow + markup + data-slots; this becomes presentational)
    - app/(app)/actuals/actuals-grid.tsx (the refactored grid from Plan 02 — rowsRef/dirtyKeys/dirtyRows/apiRef; where SaveBar is rendered today)
    - lib/actions/executions.ts (saveExecutionsBatch signature, SaveBatchState, the UnitPatch shape built from dirty rows)
    - .planning/phases/03_1-actuals-grid-refinements/03_1-RESEARCH.md §GRID-12 + R6 (double-submit + sticky/overflow + z-index)
  </read_first>
  <action>
    Create `lib/actuals/use-save-executions.ts` exporting `useSaveExecutions(getDirtyUnits, activityKey, periodId, onResult)` — ONE `useActionState<SaveBatchState, void>` whose action calls `saveExecutionsBatch(undefined, { activity: activityKey, periodId, units: getDirtyUnits() })`; an effect fires `onResult(state)` when state !== INITIAL_STATE; return `{ submit, pending, state }`. This is the SINGLE source of truth for the save flow.
    Make save-bar.tsx PRESENTATIONAL: change its props to `{ dirtyCount, pending, lastResult, onSave, slot }` (no internal useActionState, no saveExecutionsBatch import). Render the SAME markup/Tailwind classes as today (the design-reviewed Phase 3 bar) — keep the inner data-slots `unsaved-count`, `save-button`, `save-confirmation`, the saved-flash, the conflict count, the error text NESTED inside the bar root. Set the bar ROOT element's `data-slot={slot}` where `slot` is `"save-bar-top"` or `"save-bar-bottom"` (R6). IMPORTANT (plan-checker MEDIUM): the bar root must NO LONGER emit the bare `data-slot="save-bar"` — it is replaced by the top/bottom variants. Because the inner slots (`save-button`, `unsaved-count`, `save-confirmation`) now appear TWICE in the DOM (once per bar), every e2e selector for them MUST be scoped under a specific bar root, e.g. `[data-slot="save-bar-bottom"] [data-slot="save-button"]` — Task 3 updates the existing spec accordingly. The button calls `onSave` and is disabled on `pending || dirtyCount===0`.
    In actuals-grid.tsx: call `useSaveExecutions` ONCE (build the UnitPatch[] from `dirtyRows` exactly as the old SaveBar did — rowKey/planRowId/executionId/version/fields/isPlaceholder/popLines). Render TWO `<SaveBar>` instances sharing the same submit/pending/dirtyCount/lastResult: the TOP one with `sticky top-0 z-30` ABOVE the grid container `<div style={{height:600}}>` and BELOW `<FilterBar>`; the BOTTOM one keeps `sticky bottom-0`. CRITICAL (R6): render BOTH bars as siblings of the grid container INSIDE ActualsGrid's flex column — NOT inside page.tsx's `overflow-x-auto` wrapper (a sticky element inside an overflow ancestor sticks to that box, not the viewport). Give the top bar `z-30` to clear AG Grid's header stacking context. Wire onSaveResult (the existing handleSaveResult) through the hook's onResult so dirty clears once. Add ONE `keydown` listener (window) in a useEffect: `if ((e.metaKey||e.ctrlKey) && e.key.toLowerCase()==='s') { e.preventDefault(); if (dirtyKeys.size>0 && !pending) submit(); }`, deps `[dirtyKeys.size, pending, submit]`, cleaned up on unmount. ONE listener, same submit — works regardless of which bar is visible.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint "app/(app)/actuals/save-bar.tsx" "app/(app)/actuals/actuals-grid.tsx" lib/actuals/use-save-executions.ts</automated>
  </verify>
  <done>
    use-save-executions.ts has ONE useActionState. save-bar.tsx is presentational (props in, data-slot save-bar-top/-bottom). actuals-grid.tsx renders two bars sharing one submit/pending/dirtyCount (top sticky top-0 z-30 above the grid, bottom sticky bottom-0, both siblings of the grid container — not inside overflow-x-auto). ONE Ctrl/Cmd+S listener calls the same submit. tsc + eslint pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GRID-13 — coerceForKind + DOM paste-block listener (applyTransaction bulk write)</name>
  <read_first>
    - app/(app)/actuals/actuals-grid.tsx (the refactored grid — rowsRef/dirtyKeys/apiRef; add a gridWrapRef + paste useEffect)
    - lib/actuals/calc.ts (num() to reuse for number/currency; setOverride for derived-cell paste — LOCKED)
    - lib/activities/types (FieldDef.kind union) + lib/activities/counter-wall.ts (kinds in context)
    - .planning/phases/03_1-actuals-grid-refinements/03_1-RESEARCH.md §GRID-13 (the full handler shape, coercion rules, R5, R7) + §AG Grid Community Capability Table
  </read_first>
  <behavior>
    - coerceForKind("1,234", "number") → 1234; coerceForKind("₹500", "currency") → 500 (reuse num).
    - coerceForKind("31/12/25", "date") → "31/12/25" (DD/MM/YY passthrough, NEVER ISO).
    - coerceForKind("19.0760", "lat") → "19.0760" (string, NEVER numeric-coerce coordinates); same for "long".
    - coerceForKind("In Progress", "status") → "In Progress" (as-is string).
    - coerceForKind("hello", "text") → "hello".
  </behavior>
  <action>
    Create `lib/actuals/coerce.ts` exporting `coerceForKind(raw: string, kind: FieldDef["kind"]): unknown`: number/currency → `num(raw)` (export `num` from calc.ts or duplicate the 3-line guard); date → return raw string AS-IS (DD/MM/YY; do NOT convert to ISO); text/lat/long → raw string as-is (NEVER numeric-coerce coordinates — existing PITFALL); status/enum → raw string as-is (let the next save's server validation handle membership — keep paste forgiving). Add `coerce.test.ts` proving each kind per the behavior block above.
    In actuals-grid.tsx, add `const gridWrapRef = useRef<HTMLDivElement>(null)` on the grid container div, and a useEffect attaching a `paste` listener to that ref (NOT document — scope to grid focus; NOT processDataFromClipboard which is Enterprise, R7). On paste: read `e.clipboardData?.getData("text/plain")`; if empty return. Get `api.getFocusedCell()` (anchor); if none, return; else `e.preventDefault()`. Parse TSV: strip a trailing newline, `split(/\r\n|\n|\r/)` for rows, `split("\t")` for columns → 2-D array. Build the ordered EDITABLE columns at/after the anchor: `api.getAllDisplayedColumns()`, find the anchor index by `getColId()`, slice from there, filter to `def.editable === true && typeof def.field === "string" && def.field.startsWith("fields.")` (this SKIPS read-only plan.* columns — security + default rule). Walk displayed rows from the anchor down via `api.getDisplayedRowAtIndex(focused.rowIndex + i)` (R5 — displayed index respects sort/filter; never map onto rowData array indices). For each pasted cell, derive the target column key (strip "fields."), then resolve its `FieldDef.kind` via `kindOf(col)` — define this helper INSIDE actuals-grid.tsx (do NOT modify colDefs.ts here — it's outside this plan's files_modified and owned by Plans 01/05): build a `kindByKey` lookup once from the already-resolved `activityCfg` (the component already calls `getActivity(activityKey)`), e.g. `const kindByKey = useMemo(() => new Map(activityCfg.actualColumns.map(f => [f.key, f.kind])), [activityCfg])`, and `kindOf = (col) => kindByKey.get(strippedKey) ?? "text"` (default to "text" for any unmapped column — safe, no numeric coercion). Then `coerceForKind(value, kindOf(col))`, write into a cloned `newFields`; if the column has a valueGetter (derived) and the coerced value != null → `setOverride(newFields, key, true)` (LOCKED — same as manual edit). Set `isPlaceholder:false, dirty:true`, write into rowsRef, push to `changed[]`. Count `dropped` cells that fall off the right edge (no editable column) or bottom (no displayed row). After the loop: ONE `api.applyTransaction({ update: changed })` (batched, GRID-09 interaction), add all changed rowKeys to dirtyKeys in one setState, and if dropped>0 surface a small inline note/toast: "Pasted block; N cell(s) outside the editable area were ignored." (planner copy; in-system styling — neutral/amber inline note, match existing data-slot conventions). The handler writes ONLY fields.* on EXISTING rows — it can NEVER introduce an SFID (security invariant LOCKED). Pasting into a placeholder promotes it (default status from D3.1-03 persists if status wasn't pasted).
  </action>
  <verify>
    <automated>npx vitest run lib/actuals/coerce.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>
    coerce.ts + coerce.test.ts cover number/currency (num), date (DD/MM/YY passthrough), lat/long/text (no numeric coercion), status/enum (as-is). actuals-grid.tsx has a DOM paste listener on the grid container ref that parses TSV, anchors on getFocusedCell, maps onto editable fields.* columns + displayed rows, coerces per kind, sets override on derived cells, writes the whole block via ONE applyTransaction, marks rows dirty, drops overflow silently with a note. No plan.* write; no SFID introduction. `npx vitest run lib/actuals/coerce.test.ts` and `npx tsc --noEmit` pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify top+bottom save bars, Ctrl/Cmd+S, and paste-block behaviour</name>
  <action>PAUSE for the user to run the dev server and manually verify GRID-12 (two bars, shared count, Ctrl/Cmd+S) and GRID-13 (paste-block) per the steps below; do not proceed until they respond to the resume signal.</action>
  <what-built>
    GRID-12: a sticky save bar at the top of the grid mirroring the bottom one (same dirty count, same Save, same Ctrl/Cmd+S), sharing ONE save flow. GRID-13: paste a rectangular block copied from Excel/Sheets into the grid at a focused cell — it fills across editable columns and down rows, marks them dirty, and overflow is ignored with a note.
  </what-built>
  <how-to-verify>
    1. `npm run dev`, open `/actuals?activity=counter-wall` on a period with several plan rows.
    2. Make an edit — confirm BOTH a top bar (above the grid) and the bottom bar appear with the SAME unsaved count.
    3. Click Save on the TOP bar — confirm it saves and BOTH bars clear (no double-submit, counts don't diverge). Repeat clicking the BOTTOM bar on a fresh edit.
    4. With unsaved edits, press Ctrl+S (or Cmd+S on Mac) — confirm it saves (and the browser's Save dialog does NOT appear).
    5. In Excel/Sheets, copy a 2x3 block (e.g. numbers). Click a single editable cell in the grid (e.g. "Actual Sq Ft") to focus it (single click, no edit mode), then paste (Ctrl+V) — confirm the block fills across the editable columns and down the rows, the affected rows go dirty, and read-only plan columns are untouched.
    6. Copy a block WIDER than the remaining editable columns and paste — confirm the overflow is silently dropped and the "N cell(s) ignored" note appears.
    7. Apply a filter or sort, then paste — confirm the block lands on the rows you actually see (displayed order), not the wrong rows.
    8. Click Save — confirm pasted rows persist.
  </how-to-verify>
  <resume-signal>Type "approved" if both bars share one count, Ctrl/Cmd+S saves, and paste fills editable cells / respects filter / drops overflow with a note — or describe the issue.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: e2e — two save bars (shared count, no double-submit) + synthetic paste-block</name>
  <read_first>
    - e2e/actuals.spec.ts (FULL — existing selectors, fixtures, how it focuses/edits cells, the window.__actualsGridApi dev hook)
    - app/(app)/actuals/save-bar.tsx + actuals-grid.tsx (data-slot save-bar-top/-bottom, the paste listener target)
    - .planning/phases/03_1-actuals-grid-refinements/03_1-RESEARCH.md §GRID-13 e2e (synthetic ClipboardEvent + DataTransfer) + A4
  </read_first>
  <action>
    FIRST — repair the now-ambiguous existing selectors (plan-checker MEDIUM, REQUIRED — without this the previously-green tests go red at wave merge): the existing spec selects bare `[data-slot="save-bar"]` (~line 150), `[data-slot="unsaved-count"]` (~152), `[data-slot="save-button"]` (~156, ~250, and the second-page assertion ~285), and `[data-slot="save-confirmation"]` (~161, ~253, ~286). After Task 1 renders TWO bars these resolve to TWO elements (or zero for the removed bare `save-bar`), tripping Playwright strict-mode. Update each of these existing locators to scope to the BOTTOM bar — replace `[data-slot="save-bar"]` with `[data-slot="save-bar-bottom"]`, and prefix the inner ones as `[data-slot="save-bar-bottom"] [data-slot="save-button"]` / `… unsaved-count` / `… save-confirmation`. Preserve each existing test's intent and assertions; only the selector scoping changes. Do NOT delete or weaken any existing assertion.
    THEN append new tests. Test A (GRID-12): make an edit, assert BOTH `[data-slot="save-bar-top"]` and `[data-slot="save-bar-bottom"]` are visible with the SAME `unsaved-count` text (scope each count under its bar root); click the top bar's `[data-slot="save-bar-top"] [data-slot="save-button"]`; assert both bars clear and a save-confirmation shows (proves single source of truth, no divergence). Test B (GRID-13): focus an anchor cell with a single `.click()` (NOT dblclick — focus without entering edit mode), then dispatch a synthetic paste via `page.evaluate`: build a `DataTransfer`, `dt.setData("text/plain", "10\t20\n30\t40")`, construct `new ClipboardEvent("paste", { clipboardData: dt, bubbles: true, cancelable: true })`, and dispatch it on the grid container element (`[data-slot="actuals-grid"]` or the gridWrapRef element) — this avoids real-clipboard permissions (A4). Assert the target editable cells received the pasted values (read via the grid or cell text) and the rows are dirty (unsaved-count increased). Use the existing `window.__actualsGridApi` dev hook + ensureColumnVisible if a target column is off-screen.
  </action>
  <verify>
    <automated>npx playwright test e2e/actuals.spec.ts</automated>
  </verify>
  <done>
    Existing actuals e2e tests STAY GREEN with two bars rendered — their bare `save-bar`/`save-button`/`unsaved-count`/`save-confirmation` selectors are re-scoped to `save-bar-bottom` (no strict-mode violation). New Test A (both bars visible, same count, top-bar save clears both — single source of truth) and Test B (synthetic ClipboardEvent paste fills editable cells, rows go dirty) added. `npx playwright test e2e/actuals.spec.ts` passes for BOTH the updated existing tests and the new ones.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clipboard → grid | Untrusted pasted TSV crosses into cell writes; must only touch editable fields.* on existing rows |
| two save bars → one mutation | Two UI entry points must not double-submit or diverge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03_1-10 | Tampering | paste handler scope | mitigate | Paste writes ONLY fields.* (filter `field.startsWith("fields.")`) on EXISTING displayed rows; plan.* skipped; cannot introduce an SFID — off-plan guard preserved |
| T-03_1-11 | Tampering | pasted-cell coordinates/dates | mitigate | coerceForKind never numeric-coerces lat/long; date stays DD/MM/YY (no ISO); save still runs server applyServerCalc + Zod |
| T-03_1-12 | Elevation/DoS | double-submit via two bars | mitigate | ONE useActionState shared by both bars; button disabled on pending; Ctrl/Cmd+S checks `!pending` (R6) |
| T-03_1-13 | Tampering | derived-cell paste | mitigate | Pasting into a derived cell sets override via setOverride (same path as manual edit, LOCKED); server still trust-recomputes non-overridden cells on save |
| T-03_1-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — DOM ClipboardEvent + existing ag-grid-community + React |
</threat_model>

<verification>
- `npx tsc --noEmit` + eslint pass on save-bar.tsx, actuals-grid.tsx, use-save-executions.ts.
- `npx vitest run lib/actuals/coerce.test.ts` passes.
- `npx playwright test e2e/actuals.spec.ts` passes (two-bar + paste tests).
- Manual checkpoint confirms shared count / Ctrl-S / paste behaviour.
- Full suite green at wave merge.
</verification>

<success_criteria>
- Top + bottom save bars share ONE useActionState / submit / dirty count (no double-submit, no divergence); Ctrl/Cmd+S triggers the same save.
- Top bar renders outside page.tsx's overflow-x-auto wrapper with z-30 (sticks to viewport).
- Paste-block fills editable fields.* columns across + displayed rows down, coerced per kind, derived cells get override, overflow dropped with a note, plan.* never written, no SFID introduced.
- coerce.test.ts + e2e green.
</success_criteria>

<output>
Create `.planning/phases/03_1-actuals-grid-refinements/03_1-04-SUMMARY.md` when done. Note the data-slot contract (save-bar-top/-bottom) and the paste handler's security invariant (fields.* on existing rows only) for the verifier.
</output>
