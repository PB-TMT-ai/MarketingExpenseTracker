---
phase: 03_1-actuals-grid-refinements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/db/schema.ts
  - lib/db/plan-rows.ts
  - drizzle/0002_*.sql
  - drizzle/meta/_journal.json
  - drizzle/meta/0002_snapshot.json
  - lib/actuals/rows.ts
  - lib/actuals/rows.test.ts
  - lib/actuals/colDefs.test.ts
  - lib/db/migrate-0002.test.ts
autonomous: true
requirements: [GRID-10, GRID-11]
must_haves:
  truths:
    - "New placeholder rows default fields.status to 'In Progress'"
    - "'+ add unit' clones default fields.status to 'In Progress'"
    - "After migration, no execution row has status IS NULL (NULL backfilled to 'In Progress')"
    - "Every actual column is editable regardless of row status (no lock-on-Done)"
  artifacts:
    - path: "drizzle/0002_*.sql"
      provides: "plan_rows source/exception_reason/created_via/created_at DDL + executions.status backfill DML"
      contains: "UPDATE \"executions\" SET \"status\" = 'In Progress'"
    - path: "lib/actuals/rows.ts"
      provides: "DEFAULT_STATUS const + status default in placeholder branch and cloneUnitForAdd"
      contains: "In Progress"
    - path: "lib/db/migrate-0002.test.ts"
      provides: "PGlite backfill unit test (NULL→In Progress, non-NULL untouched, idempotent)"
  key_links:
    - from: "drizzle/meta/_journal.json"
      to: "drizzle/0002_*.sql"
      via: "drizzle-kit generate journal entry"
      pattern: "0002_"
    - from: "lib/actuals/rows.ts"
      to: "buildRowModel placeholder branch + cloneUnitForAdd"
      via: "DEFAULT_STATUS"
      pattern: "status.*In Progress"
---

<objective>
Land the schema foundation and the pure-lib defaults for Phase 3.1, in a single Wave-1 plan that owns the ONE shared migration `drizzle/0002`.

This plan delivers:
- GRID-10: status defaults to "In Progress" on placeholder rows and "+ add unit" clones, plus a one-time forward backfill of `executions.status IS NULL → 'In Progress'`.
- GRID-11: confirm (re-grep) that no lock-on-Done predicate exists, and add a regression test asserting editability is status-independent.
- The SCHEMA HALF of COMP-04: the four new `plan_rows` columns (`source`, `exception_reason`, `created_via`, `created_at`) ship in the SAME migration `0002` as the GRID-10 backfill (research §Migration Shape — one file for atomicity). COMP-04's backend logic (Plan 03) depends on these columns existing.

Purpose: Every downstream plan that needs the new `plan_rows` columns (Plan 03 backend, Plan 05 frontend pill) or the default-status rule (Plan 04 paste, Plan 05 modal) builds on this. Owning the migration in exactly one plan prevents journal/snapshot conflicts.

Output: Migration `0002_*.sql` (DDL + backfill DML) journaled by drizzle-kit; `schema.ts` updated with the four columns; `rows.ts` with `DEFAULT_STATUS`; updated `rows.test.ts` (R8 breaking assertions); new regression test in `colDefs.test.ts`; new `migrate-0002.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03_1-actuals-grid-refinements/03_1-CONTEXT.md
@.planning/phases/03_1-actuals-grid-refinements/03_1-RESEARCH.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. Use directly; no exploration. -->

From lib/db/schema.ts — current plan_rows (NO created_at exists today; columns to ADD are source/exception_reason/created_via/created_at):
```typescript
export const planRows = pgTable("plan_rows", {
  id, periodId, activity, sfid,
  region, state, district, taluka, distributor, dealer,
  plannedCost: numeric("planned_cost", { precision: 14, scale: 2 }),
  fields: jsonb("fields").notNull().default({}),
}, (t) => [
  unique("plan_rows_match_key").on(t.periodId, t.activity, t.sfid),
  index("plan_rows_filter_idx").on(t.periodId, t.activity, t.region, t.state, t.district),
]);
// executions.status is text("status") — nullable, NO .default() (keep it that way; app is the source of truth).
```

From lib/db/plan-rows.ts — PlanRowRecord type (extend with `source: string`):
```typescript
export type PlanRowRecord = {
  id: number; periodId: number; activity: string; sfid: string;
  region: string | null; state: string | null; district: string | null;
  taluka: string | null; distributor: string | null; dealer: string | null;
  plannedCost: string | null; fields: Record<string, unknown>;
};
// listByPeriodActivity does `select()` (all columns) → `source` flows through automatically once the DB column + this type field exist.
```

From lib/actuals/rows.ts — the two injection points (placeholder branch builds `fields: {}`; cloneUnitForAdd builds `fields: {}`):
```typescript
export type ExecutionRecord = { id; planRowId; status: string | null; ... };
export function buildRowModel(planRows: PlanRowRecord[], executions: ExecutionRecord[]): UnitRow[]; // placeholder branch: fields: {}
export function cloneUnitForAdd(row: UnitRow): UnitRow; // returns fields: {}
```

From lib/actions/executions.test.ts — the PGlite test harness to mirror for migrate-0002.test.ts:
```typescript
// 3 vi.mock blocks (next/headers, next/cache, ../auth/session) BEFORE non-vi imports — needed only if the test touches a Server Action; a pure-migration test may omit them.
beforeAll(async () => { await ensureMigrated(); }); // applies all migrations incl. 0002
// FK-safe reset order: _resetExecutionItemsForTest → _resetExecutionsForTest → _resetPlanRowsForTest → _resetPeriodsForTest
// db.execute(sql`...`) for raw seed/select; insertPeriod from ../db/periods; _findPlanRowIdForTest from ../db/plan-rows
```

Migration mechanism (verified): `npm run db:generate` (drizzle-kit generate) emits `drizzle/0002_*.sql` + `drizzle/meta/0002_snapshot.json` + appends to `drizzle/meta/_journal.json`. It emits DDL only — the backfill UPDATE must be HAND-APPENDED to the generated file after a `--> statement-breakpoint`. Statements in existing files are separated by `--> statement-breakpoint`. `ensureMigrated()` (lib/db/migrate.ts) applies every journaled migration to PGlite in order.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add COMP-04 plan_rows columns to schema + generate migration 0002 + hand-append GRID-10 backfill</name>
  <read_first>
    - lib/db/schema.ts (the planRows table + the existing pgEnum/check imports)
    - drizzle/0001_wealthy_caretaker.sql (the existing migration file format + statement-breakpoint convention)
    - drizzle/meta/_journal.json (journal entries 0000, 0001 — 0002 must be appended by generate, never hand-edited)
    - drizzle.config.ts (out: "./drizzle", dialect postgresql, driver pglite)
    - .planning/phases/03_1-actuals-grid-refinements/03_1-RESEARCH.md §Migration Shape + §COMP-04 schema DDL + R9 + R11
  </read_first>
  <action>
    In schema.ts, add four columns to the planRows table definition (use `text` + CHECK, NOT pgEnum — matches the `status` precedent, research recommendation): `source: text("source").notNull().default("plan-upload")`, `exceptionReason: text("exception_reason")`, `createdVia: text("created_via")`, `createdAt: timestamp("created_at", { withTimezone: true }).defaultNow()`. Add a `check("plan_rows_source_check", sql\`...\`)` in the table's second-arg array constraining source to IN ('plan-upload','exception'). Import `check` from drizzle-orm/pg-core if not already imported. Do NOT touch `executions.status` (it stays nullable, no DB default — the app is the source of truth per D3.1-03).
    Then run `npm run db:generate` to emit `drizzle/0002_*.sql` + `drizzle/meta/0002_snapshot.json` + the `_journal.json` 0002 entry. Verify the emitted SQL contains the four ADD COLUMN statements and the source CHECK constraint. The `DEFAULT 'plan-upload'` on source backfills every existing plan_row automatically (no separate UPDATE needed for the source column).
    HAND-APPEND the GRID-10 backfill DML to the generated `0002_*.sql` after a `--> statement-breakpoint` (drizzle-kit generate does NOT emit data DML — R9): the literal statement `UPDATE "executions" SET "status" = 'In Progress' WHERE "status" IS NULL;`. This is forward-only (D3.1-07), idempotent (second pass matches 0 rows), no rollback.
    Do NOT hand-create the .sql without the generated journal/snapshot (R11) — generate first, then append the one DML line.
  </action>
  <verify>
    <!-- Do NOT re-run `npm run db:generate` here: generation already happened in <action>, and a
         second generate can overwrite 0002_*.sql and DROP the hand-appended backfill DML (plan-checker
         MEDIUM). This verify only READS the already-generated, hand-edited file + journal. -->
    <automated>node -e "const fs=require('fs');const f=fs.readdirSync('drizzle').find(n=>n.startsWith('0002')&&n.endsWith('.sql'));if(!f)throw new Error('0002 .sql not found — run npm run db:generate in the action step first');const s=fs.readFileSync('drizzle/'+f,'utf8');if(!/ADD COLUMN.*"source"/i.test(s))throw new Error('source col missing');if(!/plan_rows_source_check|CHECK.*source/i.test(s))throw new Error('source CHECK missing');if(!/exception_reason/i.test(s))throw new Error('exception_reason missing');if(!/created_via/i.test(s))throw new Error('created_via missing');if(!/created_at/i.test(s))throw new Error('created_at missing');if(!/UPDATE \"executions\" SET \"status\" = 'In Progress' WHERE \"status\" IS NULL/.test(s))throw new Error('backfill DML missing — hand-append it after a statement-breakpoint');const j=JSON.parse(fs.readFileSync('drizzle/meta/_journal.json','utf8'));if(!j.entries.some(e=>e.tag.startsWith('0002')))throw new Error('0002 not journaled');console.log('OK');"</automated>
  </verify>
  <done>
    schema.ts declares source/exception_reason/created_via/created_at + source CHECK on planRows. `drizzle/0002_*.sql` exists, is journaled in `_journal.json`, contains the four ADD COLUMN statements, the source CHECK, and the hand-appended `UPDATE "executions" SET "status" = 'In Progress' WHERE "status" IS NULL;` after a statement-breakpoint. `executions.status` is unchanged (still nullable, no DB default).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GRID-10 — default "In Progress" in rows.ts (both branches) + PlanRowRecord.source + fix breaking rows.test.ts assertions</name>
  <read_first>
    - lib/actuals/rows.ts (buildRowModel placeholder branch ~line 145-156 builds `fields: {}`; cloneUnitForAdd ~line 202-213 returns `fields: {}`; planContext object ~line 130-141)
    - lib/actuals/rows.test.ts (lines 76-82 "placeholder row has empty fields" + lines 211-217 "cloned row has empty fields" — these WILL break, R8)
    - lib/db/plan-rows.ts (PlanRowRecord type ~line 23-36 — add `source: string`)
    - .planning/phases/03_1-actuals-grid-refinements/03_1-RESEARCH.md §GRID-10 + R8
  </read_first>
  <behavior>
    - buildRowModel placeholder branch: a plan_row with zero executions produces a placeholder row whose `fields.status === "In Progress"`.
    - cloneUnitForAdd: the returned clone has `fields.status === "In Progress"` (and still no copied execution data — only status is seeded).
    - Existing execution rows are unaffected (their status comes from the merged ExecutionRecord, not the default).
    - PlanRowRecord carries `source: string` (so listByPeriodActivity surfaces it; consumed by Plan 03/05).
  </behavior>
  <action>
    In rows.ts, add a module-level `const DEFAULT_STATUS = "In Progress";` (single symbol referenced by both injection points — research §GRID-10). In the buildRowModel placeholder branch, change `fields: {}` to `fields: { status: DEFAULT_STATUS }`. In cloneUnitForAdd, change `fields: {}` to `fields: { status: DEFAULT_STATUS }`. Do NOT add a per-activity status default (deferred). Do NOT change `executions.status` handling or the real-execution merge branch.
    Add `source: string` to the PlanRowRecord type in plan-rows.ts (the column now exists from Task 1; `listByPeriodActivity`'s `select()` returns it automatically — only the type needs the field). Do NOT yet add `source` to rows.ts planContext — that is Plan 03's task (avoids file conflict scope creep here; this plan only adds the type field).
    Update the TWO breaking tests in rows.test.ts (R8): "placeholder row has empty fields" (line ~76-82) and "cloned row has empty fields" (line ~211-217). Change them to assert `fields.status === "In Progress"` and that the only non-`__overrides`, non-`status` keys are absent (i.e. no execution data copied). Add a new positive assertion in each describe block: placeholder default status, clone default status.
  </action>
  <verify>
    <automated>npx vitest run lib/actuals/rows.test.ts</automated>
  </verify>
  <done>
    rows.ts has DEFAULT_STATUS="In Progress" referenced by both the placeholder branch and cloneUnitForAdd. PlanRowRecord declares `source: string`. rows.test.ts passes with updated assertions: placeholder and clone both have `fields.status === "In Progress"`, clone still copies no execution measurement data. `npx vitest run lib/actuals/rows.test.ts` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: GRID-11 regression test (status-independent editability) + migration 0002 backfill unit test</name>
  <read_first>
    - lib/actuals/colDefs.ts (every `editable` is a static boolean: plan false, actual true, derived true — confirm no status predicate)
    - lib/actuals/colDefs.test.ts (existing structure; add a new describe block at the end)
    - lib/actions/executions.test.ts (the PGlite harness: beforeAll ensureMigrated, FK-safe reset order, db.execute(sql`...`), insertPeriod, _findPlanRowIdForTest — mirror for migrate-0002.test.ts)
    - lib/db/migrate.ts (ensureMigrated applies all journaled migrations incl. 0002)
    - .planning/phases/03_1-actuals-grid-refinements/03_1-RESEARCH.md §GRID-11 + §Migration Shape test pattern + D3.1-00 + R1
  </read_first>
  <action>
    RE-GREP FIRST (D3.1-00 / R1 planner directive — confirm the reconciliation holds on this working branch): grep `app/` and `lib/` for `resolveEditable`, `=== 'Done'` / `=== "Done"`, `metaKey`, `ctrlKey`. Expect ZERO source hits. If any lock predicate HAS reappeared, the fix is trivial: delete the function, restore `editable: true` boolean in colDefs.ts — then proceed. Record the grep result in the SUMMARY.
    Add a regression describe block to colDefs.test.ts: "GRID-11: editability is status-independent (no lock-on-Done)". Assert that for `getActivity("counter-wall")`, every column whose `field` startsWith `"fields."` has `editable === true` AND `typeof editable === "boolean"` (the boolean check guards against a function-valued predicate creeping back). This is a static assertion because editable is a static boolean — there is no per-row status evaluation to exercise.
    Create lib/db/migrate-0002.test.ts (mirror the executions.test.ts harness). `beforeAll(ensureMigrated)`. Test 1: seed 3 executions with `status` NULL via `db.execute(sql\`insert into executions (plan_row_id, status) values (...)\`)` against a real plan_row (create a period + plan_row first via insertPeriod + a raw insert, following the harness), run the backfill statement `UPDATE executions SET status = 'In Progress' WHERE status IS NULL`, assert all three are now 'In Progress'. Test 2: a pre-set non-NULL row (e.g. status='Done') is untouched after the backfill. Test 3 (idempotency): run the UPDATE a second time and assert it affects 0 rows (the WHERE matches nothing). Test 4 (ship-wiring — plan-checker MEDIUM): read the journaled `drizzle/0002_*.sql` from disk and assert it contains the literal `UPDATE "executions" SET "status" = 'In Progress' WHERE "status" IS NULL` AFTER a `--> statement-breakpoint` — this ties the semantics proven in Tests 1-3 to the statement that actually ships in 0002, not just an ad-hoc statement the test runs. Use the FK-safe reset order in beforeEach. Note: because ensureMigrated already ran 0002's backfill on the empty fixtures (touching 0 rows), Tests 1-3 seed NULLs AFTER migration then run the same statement to prove the semantics deterministically; Test 4 proves the migration file is the one carrying that statement.
  </action>
  <verify>
    <automated>npx vitest run lib/actuals/colDefs.test.ts lib/db/migrate-0002.test.ts</automated>
  </verify>
  <done>
    Re-grep recorded (expected zero lock hits). colDefs.test.ts has a status-independent-editability regression block asserting every `fields.*` column is `editable === true` and `typeof === "boolean"`. migrate-0002.test.ts proves NULL→'In Progress', non-NULL untouched, idempotency (second UPDATE affects 0 rows), AND that the journaled `drizzle/0002_*.sql` literally carries the backfill statement after a statement-breakpoint (ship-wiring). `npx vitest run lib/actuals/colDefs.test.ts lib/db/migrate-0002.test.ts` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB migration → existing data | The backfill UPDATE mutates existing executions; must be forward-only + idempotent, never destructive |
| schema change → off-plan guard | Adding columns to plan_rows must NOT weaken the structural off-plan guard (executions still has no sfid column; FK unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03_1-01 | Tampering | drizzle/0002 backfill | mitigate | Forward-only, idempotent UPDATE with `WHERE status IS NULL`; unit-tested to leave non-NULL rows untouched (Task 3) |
| T-03_1-02 | Tampering | plan_rows source column | mitigate | `source` defaults to 'plan-upload' with a CHECK IN ('plan-upload','exception'); no app-writable path in this plan |
| T-03_1-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — schema + pure-lib only; no install task |
</threat_model>

<verification>
- `npm run db:generate` produces a journaled `0002_*.sql` containing the four plan_rows columns + source CHECK + the hand-appended status backfill DML.
- `npx vitest run lib/actuals/rows.test.ts lib/actuals/colDefs.test.ts lib/db/migrate-0002.test.ts` all pass.
- Full suite stays green: `npm test` (run at wave merge).
- The off-plan guard is structurally untouched: `executions` still has no `sfid` column; the FK is unchanged.
</verification>

<success_criteria>
- Migration `0002` owns BOTH the COMP-04 plan_rows DDL and the GRID-10 backfill DML, journaled by drizzle-kit (exactly one plan owns this migration).
- GRID-10: placeholder rows and add-unit clones default `fields.status = "In Progress"`; existing NULL-status executions backfill to 'In Progress'.
- GRID-11: re-grep confirms no lock-on-Done; regression test asserts `fields.*` columns are `editable === true` (boolean).
- PlanRowRecord carries `source: string` for downstream plans.
- R8 breaking assertions in rows.test.ts updated, suite green.
</success_criteria>

<output>
Create `.planning/phases/03_1-actuals-grid-refinements/03_1-01-SUMMARY.md` when done. Record: the re-grep result (R1/D3.1-00 confirmation), the generated 0002 filename, and a note that `executions.status` kept no DB default (app is source of truth).
</output>
