---
phase: 04-compliance-dashboard
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - package.json
  - app/(app)/dashboard/page.tsx
  - app/(app)/dashboard/stat-strip.tsx
  - app/(app)/dashboard/by-activity-card.tsx
  - app/(app)/dashboard/by-region-card.tsx
  - app/(app)/dashboard/exception-card.tsx
  - app/(app)/dashboard/dashboard-filter-bar.tsx
  - app/(app)/dashboard/refresh-button.tsx
  - app/(app)/page.tsx
  - app/(app)/nav-links.tsx
  - lib/actions/dashboard.ts
autonomous: false
requirements:
  - COMP-03
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-07
user_setup: []
must_haves:
  truths:
    - "Visiting `/` redirects to `/dashboard` (logged-in users land on the dashboard per DASH-01)."
    - "`/dashboard` renders the StatStrip with % executed, % cancelled, planned/executed/cancelled/pending counts, planned ₹ and actual ₹ for the active period — numbers exactly match `aggregateScopeTotals` + `computeCompleteness` outputs."
    - "Setting `?region=North` (or any FilterBar selection) re-aggregates the page and shrinks the numbers to that scope; clearing the filter restores totals — proves D-17 filter honoring end-to-end."
    - "By-activity card shows one row per activity with executions; by-region card shows one row per region. Both honor the same `DashboardFilters` payload."
    - "The Exception Spend card surfaces a non-zero count + ₹ only when an exception row exists in the active period — distinct from the headline metrics (D-07)."
    - "No active period → empty state pointing to `/periods`, mirroring `app/(app)/actuals/page.tsx:39-56` chrome."
    - "Dashboard FilterBar omits the Status facet (D-17 — the dashboard SHOWS status breakdowns, so filtering by status would be circular)."
    - "Recharts 3.x is installed with the `react-is` override applied; `npm install` exits 0 without peer-dep errors, gated behind a blocking-human checkpoint per the Package Legitimacy Audit `[ASSUMED]` disposition."
    - "Manual Refresh button calls `revalidateDashboard` Server Action which invokes `revalidatePath('/dashboard')` — eventual-consistency mitigation for Pitfall 5."
  artifacts:
    - path: "app/(app)/dashboard/page.tsx"
      provides: "RSC entry — parallel Promise.all of six aggregators, computes completeness, renders all cards"
      min_lines: 100
    - path: "app/(app)/dashboard/stat-strip.tsx"
      provides: "RSC StatStrip — % executed, % cancelled, counters, ₹"
    - path: "app/(app)/dashboard/by-activity-card.tsx"
      provides: "RSC by-activity breakdown table"
    - path: "app/(app)/dashboard/by-region-card.tsx"
      provides: "RSC by-region breakdown table"
    - path: "app/(app)/dashboard/exception-card.tsx"
      provides: "RSC exception-spend card (amber pill when count > 0)"
    - path: "app/(app)/dashboard/dashboard-filter-bar.tsx"
      provides: "Client island — Region/State/District/Distributor cascade, writes to searchParams via useRouter().replace"
    - path: "app/(app)/dashboard/refresh-button.tsx"
      provides: "Client island — single button that calls revalidateDashboard Server Action"
    - path: "lib/actions/dashboard.ts"
      provides: "revalidateDashboard Server Action — wraps revalidatePath('/dashboard')"
      exports: ["revalidateDashboard"]
    - path: "app/(app)/page.tsx"
      provides: "redirect('/dashboard')"
    - path: "app/(app)/nav-links.tsx"
      provides: "Dashboard link added as first entry"
  key_links:
    - from: "app/(app)/dashboard/page.tsx"
      to: "lib/db/dashboard.ts (six aggregators)"
      via: "Promise.all of six helper calls"
      pattern: "Promise\\.all"
    - from: "app/(app)/dashboard/page.tsx"
      to: "lib/compliance/completeness.ts"
      via: "computeCompleteness(totals)"
      pattern: "computeCompleteness"
    - from: "app/(app)/dashboard/dashboard-filter-bar.tsx"
      to: "lib/actuals/filter.ts optionsFor"
      via: "import from @/lib/actuals/filter (D-11 — single cascade source)"
      pattern: "from \"@/lib/actuals/filter\""
    - from: "app/(app)/page.tsx"
      to: "/dashboard"
      via: "next/navigation redirect()"
      pattern: "redirect\\(.dashboard.\\)"
---

<objective>
Build the **dashboard route + RSC + server-rendered cards + redirect/nav wiring**. The page composes six aggregators from Plan 04-02 via `Promise.all`, feeds `aggregateScopeTotals` output into `computeCompleteness` from Plan 04-01, and renders four server-rendered cards (StatStrip, ByActivity, ByRegion, Exception) plus the dashboard FilterBar client island that drives URL searchParams.

Also installs Recharts 3.x (gated behind a Package Legitimacy human-verify checkpoint per the `[ASSUMED]` disposition in 04-RESEARCH.md) so Plan 04-04's chart islands can import it.

This plan ships DASH-01, DASH-02, DASH-03, DASH-04, DASH-05 (stat surfaces), DASH-07 (drill-tree feed served via `aggregateByGeo` — the tree UI itself lands in Plan 04-04 as a client island consuming the same prop), plus the redirect + nav wiring (DASH-01 "on login user sees the dashboard").

Purpose: the headline compliance & spend numbers go live and obey filters; the chart islands and drill-tree island plug in next plan with no further server work.
Output: `/dashboard` route, four RSC cards, FilterBar client island, redirect from `/`, Dashboard nav link, Recharts installed, refresh affordance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-compliance-dashboard/04-CONTEXT.md
@.planning/phases/04-compliance-dashboard/04-RESEARCH.md
@.planning/phases/04-compliance-dashboard/04-PATTERNS.md
@.planning/phases/04-compliance-dashboard/04-01-PLAN.md
@.planning/phases/04-compliance-dashboard/04-02-PLAN.md

@app/(app)/actuals/page.tsx
@app/(app)/actuals/filter-bar.tsx
@app/(app)/page.tsx
@app/(app)/nav-links.tsx
@lib/actuals/filter.ts
@lib/periods/active.ts
@package.json

<interfaces>
<!-- Imports to use directly — DO NOT re-derive shapes; these were defined in 04-01 and 04-02. -->

<!-- From Plan 04-02 (lib/db/dashboard.ts): -->
<!--   import { -->
<!--     aggregateScopeTotals, aggregateByActivity, aggregateByRegion, -->
<!--     aggregateByGeo, aggregateWeeklyBuckets, aggregateExceptionTotals, -->
<!--     type DashboardFilters, type ScopeTotals, type ByActivityRow, -->
<!--     type ByRegionRow, type GeoRow, type WeekBucket, type ExceptionTotals -->
<!--   } from "@/lib/db/dashboard"; -->

<!-- From Plan 04-01 (lib/compliance/completeness.ts): -->
<!--   import { computeCompleteness } from "@/lib/compliance/completeness"; -->

<!-- From existing codebase: -->
<!--   import { getActivePeriod } from "@/lib/periods/active"; -->
<!--   import { ACTIVITIES, ACTIVITY_KEYS } from "@/lib/activities/registry"; -->
<!--   import type { ActivityKey } from "@/lib/activities/types"; -->
<!--   import { optionsFor, type FacetSelections } from "@/lib/actuals/filter"; -->

<!-- searchParams contract (RSC reads these): -->
<!--   ?activity=<key>          (optional; defaults to ACTIVITY_KEYS[0] — but null/"all" allowed) -->
<!--   ?region=<v>&region=<v>   (array; cascade root) -->
<!--   ?state=<v>&state=<v>     (array; depends on region) -->
<!--   ?district=<v>&district=<v> (array) -->
<!--   ?distributor=<v>         (array) -->
<!--   ?weeks=4|8|12            (rolling-N, optional; Plan 04-04 reads this) -->
<!--   ?mode=period|rolling     (Plan 04-04 reads this; default 'period') -->
<!--   NEVER ?status=...        (D-17 forbids status faceting on dashboard) -->

<!-- Recharts install (Plan 04-02 left package.json untouched; this plan installs it): -->
<!--   1. Edit package.json to add "overrides": { "react-is": "$react" } -->
<!--   2. npm install recharts -->
<!--   3. Pinned version: ^3.2.x (latest stable per 04-RESEARCH.md A1) -->
<!--   Gated behind a checkpoint:human-verify with gate="blocking-human" per Package Legitimacy [ASSUMED]. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify Recharts legitimacy (Package Legitimacy [ASSUMED] disposition)</name>
  <what-built>Planning step — no code yet. Researcher tagged `recharts` as `[ASSUMED]` because slopcheck did not run in the research session. Per planner directive in the system prompt, install tasks MUST be gated behind a blocking-human checkpoint when packages are `[ASSUMED]` or `[SUS]`.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/recharts in a browser.
    2. Confirm the package:
       - Is published by `recharts` org (or a maintainer linked to github.com/recharts/recharts).
       - Has > 1,000,000 weekly downloads.
       - Has a recent release (within the last 12 months).
       - Links to GitHub repo `recharts/recharts` with > 20k stars.
       - Latest stable version is in the 3.x line.
    3. Open https://github.com/recharts/recharts and confirm the repo is active, not archived.
    4. Note any drift from RESEARCH.md A1 (Recharts 3.x stable) — if the latest is now 4.x or has known React 19 issues, ESCALATE before approving.
  </how-to-verify>
  <resume-signal>Type "approved" once verified. Type "BLOCK: <reason>" if anything looks off (typosquat, archived repo, version drift, etc.) — DO NOT proceed to the install task.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install Recharts 3.x with react-is override (Pitfall 6 / R9 mitigation)</name>
  <read_first>
    - package.json (full file — current dependencies + overrides section, if any)
    - .planning/phases/04-compliance-dashboard/04-RESEARCH.md (Pitfall 6 lines 440-448; R9; Installation block lines 121-126)
  </read_first>
  <action>
    Edit `package.json`:
    1. Add a top-level `"overrides": { "react-is": "$react" }` block (or merge into an existing one — confirm via Read first). This must land BEFORE the npm install (Recharts issue #4558 — React 19 peer-dep fix).
    2. Run `npm install recharts` to add the latest 3.x. Confirm the resolved version in `package.json` and `package-lock.json`.
    3. Confirm `npm install` exits 0 with no peer-dependency errors. If a peer-dep warning surfaces, fix the override before continuing (do NOT use `--legacy-peer-deps`).

    DO NOT add `@types/recharts` (Recharts 3.x ships its own types). DO NOT pin via heredoc CDN tarball (Recharts is npm-safe, unlike `xlsx`).
  </action>
  <verify>
    <automated>node -e "const p = require('./package.json'); if (!p.dependencies.recharts) process.exit(1); if (!p.overrides || p.overrides['react-is'] !== '$react') process.exit(2); console.log('OK', p.dependencies.recharts);"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"recharts"` under `dependencies` at a 3.x version.
    - `package.json` contains `"overrides": { "react-is": "$react" }`.
    - `package-lock.json` resolves `recharts` to a 3.x version.
    - `npm install` exits 0 (verifiable: `npm install --dry-run` shows no peer-dep errors).
    - `node_modules/recharts/package.json` exists.
  </acceptance_criteria>
  <done>Recharts 3.x available for import in Plan 04-04's chart islands; React 19 peer-dep workaround in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build dashboard RSC + four server-rendered cards + redirect + nav + Refresh Server Action</name>
  <read_first>
    - app/(app)/actuals/page.tsx (full file — analog: dynamic='force-dynamic', searchParams parsing, Promise.all data fetch, ACTIVITY_KEYS resolution, no-active-period empty state at lines 39-56, data-slot conventions at lines 26-30)
    - app/(app)/page.tsx (12 lines — replace with redirect)
    - app/(app)/nav-links.tsx (lines 6-11 — LINKS array; add dashboard as first entry)
    - app/(app)/actuals/filter-bar.tsx (full file — analog for dashboard-filter-bar.tsx; KEY DIVERGENCE: dashboard writes to URL searchParams via useRouter, omits Status facet per D-17)
    - lib/actuals/filter.ts (lines 92-104 — optionsFor; D-11 single cascade source — REUSE, do not duplicate)
    - lib/periods/active.ts (active period fetch)
    - .planning/phases/04-compliance-dashboard/04-PATTERNS.md (lines 161-318 — page/stat-strip/filter-bar/exception-card pattern assignments)
    - .planning/phases/04-compliance-dashboard/04-RESEARCH.md (Code Examples lines 464-489 — StatStrip target shape)
  </read_first>
  <behavior>
    - Visiting `/` calls `redirect("/dashboard")` from `next/navigation`. Logged-in users land on the dashboard.
    - `app/(app)/nav-links.tsx` LINKS array prepends `{ href: "/dashboard", label: "Dashboard" }` so it renders first.
    - `/dashboard` (RSC, `export const dynamic = "force-dynamic"`) reads `searchParams`, fetches active period, parses + Zod-validates filter params, calls `Promise.all` of six aggregators, computes completeness, renders all cards.
    - When `activePeriod === null` the page renders the same empty-state chrome as `app/(app)/actuals/page.tsx:39-56` with a link to `/periods`.
    - When `totals.plannedUnits === 0` the page still renders the StatStrip (showing zeros) plus a soft empty-state note pointing to `/plans`. Does NOT short-circuit.
    - `dashboard-filter-bar.tsx` ("use client") wraps Region/State/District/Distributor cascade using `optionsFor` from `@/lib/actuals/filter` (D-11). On any facet change it calls `useRouter().replace(\`?\${new URLSearchParams(...)}\`, { scroll: false })`. NEVER renders a Status facet (D-17). The cascade-clear control flow from `app/(app)/actuals/filter-bar.tsx:92-99` is copied: changing Region clears State + District; changing State clears District.
    - `stat-strip.tsx` (RSC) renders a `<dl>` with `% Executed`, `% Cancelled`, `Planned units`, `Done`, `Cancelled`, `Pending + WIP`, `Planned ₹`, `Actual ₹`. Money formatted via `Intl.NumberFormat("en-IN", { style: "currency", currency: "INR" })`. Percentages: `${(n * 100).toFixed(1)}%`. Each `<dd>` carries a `data-slot="stat-{label-slug}"` so e2e can assert specific values.
    - `by-activity-card.tsx` (RSC) renders one table row per `ByActivityRow` with activity label (from `ACTIVITIES[key].label`, or the raw key if missing) + planned/executed/cancelled + planned/actual ₹. Empty array → muted "No activity data for this scope." note.
    - `by-region-card.tsx` (RSC) renders one row per `ByRegionRow`, region name with "(unassigned)" fallback already applied in the helper return.
    - `exception-card.tsx` (RSC) renders count + ₹ from `aggregateExceptionTotals`. When count > 0 shows an amber pill (same chrome as `app/(app)/actuals/page.tsx:156-189`): `<span className="inline-flex items-center rounded-full bg-amber-50 px-2 py-0.5 text-xs font-medium text-amber-800">{count} off-plan</span>`. When count === 0 shows a quiet "No off-plan exceptions in scope" note (still rendered, so the user always sees the bucket).
    - `lib/actions/dashboard.ts` exports a `revalidateDashboard()` Server Action: `"use server"` directive, marker function calling `revalidatePath("/dashboard")` from `next/cache`. Used by `refresh-button.tsx` ("use client") which is a single button calling the action via `formAction` or `useTransition`. Pitfall 5 / R3 mitigation per RESEARCH.
    - **Data-slot contract for e2e:** `data-slot="dashboard-page"` on page root, `"stat-strip"`, `"by-activity-card"`, `"by-region-card"`, `"exception-card"`, `"dashboard-filter-bar"`, `"refresh-button"`. Plan 04-04 will add the remaining islands.
    - **Filter parsing helper** (private to page.tsx or co-located): a small function `parseDashboardFilters(searchParams, activePeriod): DashboardFilters` that Zod-validates and normalizes. Reject `status` if present (silently strip — D-17). Activity is `null` if `?activity=all` else the resolved ActivityKey. Each list facet uses `Array.isArray(raw) ? raw : raw ? [raw] : []`.
    - **No chart islands in this plan** — the Promise.all DOES call `aggregateWeeklyBuckets` (so the data ships to the page), but the chart components themselves land in Plan 04-04. Plan 04-03 page passes weekly bucket data to a placeholder slot that Plan 04-04 will fill — render a `<section data-slot="weekly-trend-chart-placeholder" />` with a comment saying "Filled by Plan 04-04". Same for `geo-drill-tree`. This lets the e2e test in this plan exercise the data plane without depending on Recharts behavior.
  </behavior>
  <action>
    **Step A — `/` redirect.** Replace `app/(app)/page.tsx` with:
    ```
    import { redirect } from "next/navigation";
    export default function RootRedirect() { redirect("/dashboard"); }
    ```

    **Step B — Nav link.** Edit `app/(app)/nav-links.tsx`: prepend `{ href: "/dashboard", label: "Dashboard" }` to the LINKS const array as the first entry. Preserve `as const`.

    **Step C — Server Action.** Create `lib/actions/dashboard.ts` with `"use server"`, exporting `async function revalidateDashboard()` that calls `revalidatePath("/dashboard")`. Mirror discipline from existing `lib/actions/*.ts` (`requireSession()` if other actions use it — match pattern; Read one analog first).

    **Step D — RSC page.** Create `app/(app)/dashboard/page.tsx`. Use `app/(app)/actuals/page.tsx` as the structural template. Steps:
      1. `export const dynamic = "force-dynamic"`.
      2. `async function DashboardPage({ searchParams })` — same Promise-typed searchParams as actuals.
      3. `const activePeriod = await getActivePeriod()`. If null, render empty-state chrome.
      4. `const filters: DashboardFilters = parseDashboardFilters(resolvedParams, activePeriod)` — defined inline.
      5. `const [totals, byActivity, byRegion, byGeo, weekly, exceptionTotals] = await Promise.all([...six helpers...])` — feed each `aggregate*` from `@/lib/db/dashboard`. For `aggregateWeeklyBuckets` pass `{ kind: 'period' }` by default; pass `{ kind: 'rolling', weeks: N }` if `searchParams.mode === 'rolling'` and `weeks` is in `{4, 8, 12}`.
      6. `const completeness = computeCompleteness(totals)`.
      7. Render: page chrome → DashboardFilterBar + RefreshButton → StatStrip → grid of (ByActivityCard, ByRegionCard) → ExceptionCard → weekly-trend placeholder → geo-drill placeholder. Pass `byGeo` to the geo-drill placeholder slot (Plan 04-04 picks it up).
      8. `data-slot="dashboard-page"` on the root div.

    **Step E — StatStrip / ByActivity / ByRegion / Exception cards.** Create each as an RSC. Mirror the `<dl>` and card chrome from `app/(app)/actuals/page.tsx:172-189` and `:156-189`. Use the `Intl.NumberFormat("en-IN", { style: "currency", currency: "INR" })` pattern. Percentage formatter: `(n) => \`\${(n * 100).toFixed(1)}%\``. Each card receives its slice of pre-aggregated data as a typed prop — NO database access from cards.

    **Step F — DashboardFilterBar client island.** Create `app/(app)/dashboard/dashboard-filter-bar.tsx` with `"use client"`. Props: `{ initialFilters: DashboardFilters; allRows: never }` — actually, the dashboard FilterBar **does not have allRows** because it's not pulling rows client-side. Instead, the page passes pre-computed facet option lists: `regions: string[], states: string[], districts: string[], distributors: string[]` derived server-side from `aggregateByGeo`. So the prop is `{ initialFilters, options: { regions, states, districts, distributors } }`. Use `useRouter` + `useSearchParams` from `next/navigation`. On any facet change construct a new `URLSearchParams` and call `router.replace(\`?\${params}\`, { scroll: false })`. Status facet is NOT rendered (D-17). DO NOT lift state into useState — single source of truth is the URL.

    **Step G — RefreshButton client island.** Create `app/(app)/dashboard/refresh-button.tsx` with `"use client"`. Imports `revalidateDashboard` from `@/lib/actions/dashboard`. Single `<button>` that wraps the action call in `useTransition` and shows a "Refreshing…" label while pending.

    **Step H — Placeholders for Plan 04-04.** Inside the page render two empty `<section data-slot="weekly-trend-chart-placeholder" />` and `<section data-slot="geo-drill-tree-placeholder" data-rows={byGeo.length} />` blocks WITH a code comment `{/* Filled by Plan 04-04 — receives weekly={weekly} and rows={byGeo} */}`. This makes the dependency explicit for the next executor.

    DO NOT import recharts in this plan. DO NOT inline any aggregate SQL — every number flows through `@/lib/db/dashboard`. DO NOT add a status facet to dashboard-filter-bar.tsx. DO NOT use `useState` for filter state in the dashboard FilterBar (URL is the source of truth). DO NOT modify `app/(app)/actuals/filter-bar.tsx` (preserve the grid contract — D-11 reuses `optionsFor` only, not the FilterBar component itself).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test -- --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `app/(app)/page.tsx` calls `redirect("/dashboard")`.
    - `app/(app)/nav-links.tsx` LINKS array has `{ href: "/dashboard", label: "Dashboard" }` as the first entry.
    - `app/(app)/dashboard/page.tsx` exists with `export const dynamic = "force-dynamic"` and a single `Promise.all` containing six aggregator calls (grep: `grep -c "aggregate" app/\\(app\\)/dashboard/page.tsx` returns at least 6).
    - `grep -c "computeCompleteness" app/\\(app\\)/dashboard/page.tsx` returns at least 1.
    - `grep -v '^\s*\*\|^\s*//' app/\\(app\\)/dashboard/dashboard-filter-bar.tsx | grep -c "status"` returns 0 (D-17 enforcement; comments stripped so design notes don't self-invalidate).
    - `grep -c "useRouter\|router.replace" app/\\(app\\)/dashboard/dashboard-filter-bar.tsx` returns at least 1 (URL-as-source-of-truth).
    - `grep -c "from \"@/lib/db/dashboard\"" app/\\(app\\)/dashboard/page.tsx` returns at least 1.
    - `grep -c "data-slot=\"dashboard-page\"" app/\\(app\\)/dashboard/page.tsx` returns 1.
    - `grep -c "data-slot=\"weekly-trend-chart-placeholder\"" app/\\(app\\)/dashboard/page.tsx` returns 1.
    - `grep -c "data-slot=\"geo-drill-tree-placeholder\"" app/\\(app\\)/dashboard/page.tsx` returns 1.
    - `lib/actions/dashboard.ts` exists and exports `revalidateDashboard`.
    - `npx tsc --noEmit` exits 0.
    - `npm test -- --no-file-parallelism` exits 0 (no regression to Phase 1-3 tests).
  </acceptance_criteria>
  <done>Server-rendered dashboard live with all four card surfaces, FilterBar driving URL state, redirect + nav wiring complete, placeholders ready for Plan 04-04 to drop in chart + tree islands.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify dashboard renders with non-zero data in browser</name>
  <what-built>
    `/dashboard` route renders StatStrip + ByActivity + ByRegion + Exception cards + FilterBar + RefreshButton, driven by aggregators from Plan 04-02 and completeness math from Plan 04-01. Visiting `/` redirects to `/dashboard`. Recharts is installed but not yet imported (Plan 04-04).
  </what-built>
  <how-to-verify>
    1. `npm run dev` — confirm it starts without errors.
    2. Visit http://localhost:3000 and confirm redirect to `/dashboard` (browser URL changes).
    3. If no active period: empty state shows a link to `/periods`. Create a period and mark active.
    4. If no plan rows: visit `/plans` and upload a small plan for an activity (use an existing test fixture if available).
    5. Visit `/actuals` and record a few executions: at least one Done, one In Progress, one Pending, and (if Cancelled is available in the registry after Plan 04-01 ships) one Cancelled.
    6. Return to `/dashboard`. Verify:
       - StatStrip shows non-zero `% Executed`, `% Cancelled`, planned/executed/cancelled/pending counts, planned ₹ and actual ₹.
       - `% Executed = executed / (planned - cancelled)` matches a hand-calc.
       - `% Cancelled = cancelled / planned` matches a hand-calc (different denominator — D-04 asymmetry visible).
       - By-activity card lists at least one row.
       - By-region card lists at least one row (or "(unassigned)" if no region on plan rows).
       - Exception card shows "No off-plan exceptions in scope" if no exception rows exist; if you previously added one via Phase 3.1, it surfaces here with an amber pill.
       - Two placeholder sections render but are visually empty (intentional — Plan 04-04 fills them).
    7. Select a Region in the FilterBar. URL updates with `?region=...`. Stats shrink to that region.
    8. Click Refresh button. Page refetches (loading state briefly visible).
    9. Manually attempt `/dashboard?status=Done` in URL — verify the status param is silently ignored (no status facet rendered in FilterBar; stats unchanged from baseline).
  </how-to-verify>
  <resume-signal>Type "approved" once verified. Type "ISSUES: <details>" with specifics if anything is wrong (number mismatch, FilterBar regression, status facet leaked, etc.).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → RSC | URL searchParams are untrusted user input (a user can craft any `?status=...&region=...`); Zod-validation at page entry. |
| Browser → Server Action | revalidateDashboard accepts no input; trust boundary is the session cookie middleware. |
| npm registry → repo | Recharts install pulls third-party code into the build; gated by Task 1 human-verify checkpoint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | Crafted searchParams (`?status=Done` to bypass D-17, `?activity=<sql>` injection attempt) | mitigate | Zod-validate in `parseDashboardFilters`; `status` silently stripped; `activity` checked against `ACTIVITY_KEYS`; Drizzle parameterizes the rest |
| T-04-03-02 | Information disclosure | Cached page serves stale data after grid write (Pitfall 5) | mitigate | `dynamic = "force-dynamic"`; RefreshButton calls `revalidatePath('/dashboard')` |
| T-04-03-03 | Spoofing | Unauthenticated user accesses dashboard | mitigate | Existing middleware at `app/(app)/layout.tsx` already gates the `(app)` group via shared-password JWT cookie; no new auth needed |
| T-04-03-04 | Tampering | Recharts npm package compromise | mitigate | Task 1 blocking-human checkpoint per Package Legitimacy [ASSUMED]; pinned 3.x version |
| T-04-03-SC | Tampering | `npm install recharts` adds untrusted dependency code | mitigate | Task 1 (blocking-human) + Task 2 verifies override + no-peer-warning install |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npm test -- --no-file-parallelism` exits 0 (no regression).
- `npm run dev` starts; `/` redirects to `/dashboard`; dashboard renders with active period + executions present.
- Task 1 + Task 4 human-verify checkpoints approved.
</verification>

<success_criteria>
- DASH-01 + DASH-03 + DASH-05 visible via StatStrip with computed % Executed and % Cancelled.
- DASH-02 visible via ByActivity + ByRegion cards.
- DASH-04 verified by FilterBar narrowing the numbers and the URL.
- DASH-07 data plane shipped (`aggregateByGeo` props piped to the geo-drill placeholder); Plan 04-04 renders the tree UI.
- D-07 exception bucket surfaces in its own card.
- Recharts available for Plan 04-04 imports.
</success_criteria>

<output>
Create `.planning/phases/04-compliance-dashboard/04-03-SUMMARY.md` when done — record (a) Recharts version installed, (b) hand-calc of % Executed and % Cancelled from the seeded test data alongside the StatStrip readout (proves D-04 asymmetry visible), (c) confirmation that `/?status=Done` was silently ignored by the FilterBar, (d) any drift discovered between RESEARCH-assumed actuals/page.tsx patterns and HEAD.
</output>
