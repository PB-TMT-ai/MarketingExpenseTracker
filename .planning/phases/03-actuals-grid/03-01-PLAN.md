---
phase: 03-actuals-grid
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - app/(app)/spike-grid/page.tsx
  - app/(app)/spike-grid/spike-grid.tsx
autonomous: false
requirements: [GRID-01, GRID-02, GRID-03, GRID-04]
user_setup: []

must_haves:
  truths:
    - "AG Grid Community 35.3.1 renders ~800 rows in the Next 16 App Router with inline cell editing that feels instant"
    - "A custom select cell editor (Status) and an external filter both work in Community (no Enterprise key)"
    - "A GO/NO-GO decision vs the TanStack Table fallback is recorded with evidence for each spike assumption A1-A4, A6"
  artifacts:
    - path: "app/(app)/spike-grid/spike-grid.tsx"
      provides: "Throwaway AG Grid proof: ~800 synthetic rows, inline edit, select editor, external filter"
      min_lines: 60
    - path: "package.json"
      provides: "ag-grid-community + ag-grid-react pinned at 35.3.1 (exact, no caret)"
      contains: "ag-grid-community"
  key_links:
    - from: "app/(app)/spike-grid/spike-grid.tsx"
      to: "ag-grid-react"
      via: "AgGridReact import behind next/dynamic ssr:false"
      pattern: "AgGridReact"
---

<objective>
Run the D3-00 mandatory throwaway spike: prove AG Grid Community 35.3.1 works inside Next 16 App Router + React 19 before committing the real build. This is a bounded, disposable proof — NOT production code. It resolves the five tertiary-confidence assumptions the RESEARCH flagged (A1 dotted field paths, A2 module names, A3 ssr:false placement, A4 themeQuartz↔Tailwind coexistence, A6 quickFilter vs SFID predicate) and yields a GO/NO-GO verdict against the documented TanStack Table fallback.

Purpose: De-risk the one genuinely new technical surface in this phase. Every downstream plan (03-02..03-05) assumes a GO. If the spike fails its criteria, the phase pivots to TanStack Table and re-plans.
Output: Installed AG Grid deps (pinned), a throwaway `/spike-grid` route, and a recorded GO/NO-GO decision in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-actuals-grid/03-CONTEXT.md
@.planning/phases/03-actuals-grid/03-RESEARCH.md

<interfaces>
<!-- The spike is self-contained: synthetic rows, no DB. These are the ONLY codebase facts it needs. -->

Existing nav shell (the spike route lives under the (app) group so it inherits the auth gate):
- app/(app)/layout.tsx re-verifies the session on every render; any page under app/(app)/ is already password-gated.

Status enum the spike's select editor should use (mirrors D3-09 — Pending / In Progress / Done):
- ["Pending", "In Progress", "Done"]

RESEARCH §"AG Grid Setup" — the exact integration shape to reproduce (read 03-RESEARCH.md lines 452-485):
- ModuleRegistry.registerModules([AllCommunityModule]) once, before first render (v33+ requires module registration).
- next/dynamic(() => import("./spike-grid"), { ssr: false }) — AG Grid is browser-only; ssr:false avoids `window is not defined`.
- v33+ Theming API default: import NO CSS; the grid injects themeQuartz into <head> automatically. Legacy escape hatch is theme="legacy" + CSS imports — try default first.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Install AG Grid Community (legitimacy checkpoint)</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - .planning/phases/03-actuals-grid/03-RESEARCH.md (§"Package Legitimacy Audit" lines 137-156; §"Standard Stack" lines 114-129)
    - package.json (current deps — confirm ag-grid not yet present; note exact-pin convention: next/react/drizzle/zod have no caret)
    - CLAUDE.md (prescriptive stack table — AG Grid Community 35.x is the locked grid)
  </read_first>
  <action>
    slopcheck was unavailable during research, so both packages are treated as [ASSUMED] and gated here per the package-legitimacy protocol. Run `npm install ag-grid-community@35.3.1 ag-grid-react@35.3.1` (exact versions, no caret — match the project's pin convention). Then PAUSE for a human to confirm, in ~5 seconds, that the install pulled the real packages from the npm registry and ran no unexpected postinstall scripts.

    What-built: ag-grid-community@35.3.1 and ag-grid-react@35.3.1 added to package.json/package-lock.json.
    How-to-verify:
      1. Run `npm view ag-grid-community@35.3.1 dist.tarball` and confirm the URL is on registry.npmjs.org.
      2. Open https://www.npmjs.com/package/ag-grid-community and confirm: ~10-year-old package, multi-million weekly downloads, repo github.com/ag-grid/ag-grid.
      3. Confirm `package.json` shows BOTH packages at exactly "35.3.1" (identical versions — AG Grid requires the two match) and the install log shows no suspicious postinstall scripts.
    Resume-signal: Type "approved" to proceed, or describe the anomaly to abort.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');const a=p.dependencies['ag-grid-community'],b=p.dependencies['ag-grid-react'];if(a!=='35.3.1'||b!=='35.3.1'){console.error('expected both 35.3.1, got',a,b);process.exit(1)}console.log('ag-grid pinned 35.3.1 OK')"</automated>
  </verify>
  <done>Both ag-grid-community and ag-grid-react are in package.json at exactly "35.3.1"; human confirmed legitimacy at the checkpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Build the throwaway spike grid and resolve A1-A4, A6</name>
  <files>app/(app)/spike-grid/page.tsx, app/(app)/spike-grid/spike-grid.tsx</files>
  <read_first>
    - .planning/phases/03-actuals-grid/03-RESEARCH.md (§"AG Grid Setup" lines 452-485; §"Assumptions Log" lines 590-602; §"Open Questions" lines 604-624; §Pitfall 3 theming lines 526-530; §Pitfall 7 SSR lines 550-553)
    - app/(app)/layout.tsx (the auth-gated shell the spike route inherits)
    - app/(app)/plans/page.tsx (Server Component shape: `export const dynamic = "force-dynamic"`, default async function, Tailwind container classes to mirror)
  </read_first>
  <action>
    Create a DISPOSABLE spike under `app/(app)/spike-grid/`. This code is deleted (or left unreferenced) after the GO/NO-GO — do not invest in polish, abstraction, or the registry. Goal: exercise every risky AG Grid behavior against a realistic row count.

    Create `app/(app)/spike-grid/page.tsx` as a Server Component (mirror plans/page.tsx: `export const dynamic = "force-dynamic"`, default async function) that renders the dynamically-imported client grid. If Next 16 rejects `ssr:false` from a Server Component (RESEARCH A3 / Open Question 1), put the `dynamic()` call inside the `"use client"` grid file or a thin client wrapper — record which placement worked.

    Create `app/(app)/spike-grid/spike-grid.tsx` as `"use client"`:
      - Import `./` AG Grid setup inline: `ModuleRegistry.registerModules([AllCommunityModule])` from "ag-grid-community" (run once at module scope).
      - Generate ~800 synthetic rows in memory (no DB) shaped to test A1: nest fields under a `plan` object and a `fields` object on each row (e.g. `{ rowKey, plan: { region, state, sfid }, fields: { length, breadth, totalSqft, status } }`), so dotted `field: "plan.region"` / `field: "fields.length"` binding can be confirmed. If dotted paths fail, flatten to top-level keys and record that A1 = flatten-fallback.
      - Columns: a read-only plan column (`editable: false`), an editable number column (`agNumberCellEditor`), an editable Status column using `agSelectCellEditor` with `cellEditorParams.values = ["Pending","In Progress","Done"]` (proves a custom select editor works in Community), and a derived `totalSqft` column via `valueGetter` computing length×breadth (proves valueGetter + change detection refreshes on input edit — the D3-04/05 mechanic).
      - Wire an external filter: `isExternalFilterPresent` + `doesExternalFilterPass` filtering by a single Region select above the grid, plus a `quickFilterText` box bound to an SFID search input (proves A6 — note whether quickFilter over-matches other columns vs needing a dedicated SFID predicate).
      - Use `getRowId={(p) => p.data.rowKey}`.
      - Import NO AG Grid CSS first (themeQuartz default, A4). If the grid renders unstyled or clashes badly with Tailwind preflight, switch to `theme="legacy"` + the legacy CSS imports and record A4 = legacy-needed.

    Do NOT add the spike route to the nav (it is throwaway). It is reachable by direct URL `/spike-grid` for manual verification.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>`/spike-grid` renders ~800 rows; the spike file compiles; the grid exercises inline number edit, a Status select editor, a valueGetter-derived column, an external Region filter, and an SFID quick-filter.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: GO/NO-GO decision vs TanStack fallback</name>
  <files>.planning/phases/03-actuals-grid/03-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/03-actuals-grid/03-RESEARCH.md (§"Assumptions Log" lines 590-602 — A1-A7; §"Open Questions" lines 604-624)
    - .planning/phases/03-actuals-grid/03-CONTEXT.md (D3-00 — TanStack Table is the documented fallback)
  </read_first>
  <action>
    Run the dev server (`npm run dev`), log in (password `jsw-marketing-2026`), open `/spike-grid`, and evaluate the GO/NO-GO criteria. PAUSE for a human verdict.

    What-built: a throwaway AG Grid Community proof at /spike-grid with ~800 rows, inline edit, a select editor, a derived column, and an external + quick filter.
    How-to-verify (GO requires ALL true):
      1. Scrolling ~800 rows is smooth; entering edit on a cell is instant (no perceptible lag).
      2. Editing length/breadth updates the derived totalSqft cell automatically (change detection works).
      3. The Status select editor shows exactly Pending / In Progress / Done.
      4. The Region external filter narrows visible rows; the SFID quick-filter finds a known SFID.
      5. The grid is styled (themeQuartz auto-injected) and does not visually collide with the app's Tailwind layout — OR the legacy-theme escape hatch was used and looks correct.
      6. No console error about an Enterprise license / missing module.
    Record in the SUMMARY, with a one-line verdict each: A1 (dotted paths work? or flatten), A2 (AllCommunityModule sufficient?), A3 (where ssr:false had to live), A4 (default theme vs legacy), A6 (quickFilter vs dedicated SFID predicate). Then state GO or NO-GO. On NO-GO, do NOT proceed to 03-02..03-05 — return to planning for the TanStack Table fallback (D3-00).
    Resume-signal: Type "GO" (proceed to the real build) or "NO-GO: <reason>" (pivot to TanStack).
  </action>
  <verify>
    <human-check>Human confirms all six GO criteria above are met against the running /spike-grid, and a GO/NO-GO verdict plus A1/A2/A3/A4/A6 findings are recorded in the SUMMARY.</human-check>
  </verify>
  <done>A GO or NO-GO verdict is recorded in the SUMMARY with explicit findings for A1, A2, A3, A4, A6. On GO, downstream plans are unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local build | New third-party packages (ag-grid-community, ag-grid-react) enter the supply chain |
| browser → app shell | Spike route renders behind the existing shared-password gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | npm install of ag-grid packages | mitigate | slopcheck unavailable → both packages [ASSUMED]; blocking-human checkpoint (Task 1) verifies on npmjs.com + npm view tarball host + checks postinstall output before use |
| T-03-01 | Information Disclosure | /spike-grid route | accept | Spike uses synthetic in-memory data only (no DB read, no real SFIDs); route is under (app) group so the auth gate still applies. Throwaway — removed/unreferenced after GO/NO-GO |
</threat_model>

<verification>
- `node -e` pin check confirms both AG Grid packages at exactly 35.3.1.
- `npx tsc --noEmit` passes (spike compiles under React 19 types).
- Manual: `/spike-grid` renders ~800 rows with working edit, select editor, derived column, and filters behind the login.
- GO/NO-GO verdict recorded with per-assumption findings.
</verification>

<success_criteria>
- AG Grid Community 35.3.1 + ag-grid-react 35.3.1 installed (exact pin), legitimacy human-verified.
- Throwaway `/spike-grid` proves inline edit, custom select editor, valueGetter-derived cell, and external + quick filter at ~800 rows in Next 16 + React 19.
- A1, A2, A3, A4, A6 each resolved with a recorded finding; GO or NO-GO decision made.
</success_criteria>

<output>
Create `.planning/phases/03-actuals-grid/03-01-SUMMARY.md` when done. MUST record: the GO/NO-GO verdict, per-assumption findings (A1 dotted-path support, A2 module set, A3 ssr:false placement, A4 theme choice, A6 SFID-search approach), and the exact AG Grid integration recipe that worked (so 03-04 reuses it verbatim instead of rediscovering it).
</output>
